<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="3.10.0">Jekyll</generator><link href="https://hrllk.github.io/feed.xml" rel="self" type="application/atom+xml" /><link href="https://hrllk.github.io/" rel="alternate" type="text/html" /><updated>2026-04-14T09:10:25+00:00</updated><id>https://hrllk.github.io/feed.xml</id><title type="html">knowledge.log</title><subtitle>knowledge.log
</subtitle><author><name>HwiRyung Kim</name></author><entry><title type="html">Modern Beautiful API Response Design: Exception Translation Layer</title><link href="https://hrllk.github.io/modern-beautiful-api-response-design-exception-translation-layer.html" rel="alternate" type="text/html" title="Modern Beautiful API Response Design: Exception Translation Layer" /><published>2026-03-18T00:00:00+00:00</published><updated>2026-03-18T00:00:00+00:00</updated><id>https://hrllk.github.io/modern-beautiful-api-response-design-exception-translation-layer</id><content type="html" xml:base="https://hrllk.github.io/modern-beautiful-api-response-design-exception-translation-layer.html"><![CDATA[<h3 id="toc">TOC</h3>
<hr />
<ol>
  <li><a href="#overview">Overview</a></li>
  <li><a href="#왜-번역-계층이-따로-필요한가">왜 번역 계층이 따로 필요한가</a></li>
  <li><a href="#설계-목표">설계 목표</a></li>
  <li><a href="#1-번역-책임은-프레젠테이션-계층에-둔다">1. 번역 책임은 프레젠테이션 계층에 둔다</a></li>
  <li><a href="#2-advisor는-내부-계약을-외부-계약으로-바꾼다">2. Advisor는 내부 계약을 외부 계약으로 바꾼다</a></li>
  <li><a href="#3-번역-계층이-하면-안-되는-일">3. 번역 계층이 하면 안 되는 일</a></li>
  <li><a href="#closing">Closing</a></li>
</ol>

<h3 id="overview">Overview</h3>
<hr />
<p>앞선 글에서 내부 예외 모델과 외부 에러 계약을 분리했다. 이제 남은 질문은 하나다. <strong>이 둘을 어디서 연결할 것인가?</strong></p>

<p>이 글에서는 <code class="language-plaintext highlighter-rouge">ProblemDetail</code> 같은 외부 응답 계약을 실제로 누가 만들고, 왜 그 책임을 프레젠테이션 계층에 두어야 하는지를 다룬다.</p>

<h3 id="왜-번역-계층이-따로-필요한가">왜 번역 계층이 따로 필요한가</h3>
<hr />
<p>내부 예외 모델과 외부 응답 계약은 역할이 다르다.</p>

<ul>
  <li>내부는 아키텍처와 도메인 책임을 지키기 위한 모델이다.</li>
  <li>외부는 클라이언트와의 안정적인 통신 계약이다.</li>
</ul>

<p>이 둘을 같은 계층에서 처리하면 내부 설계가 외부 포맷에 끌려가고, 외부 포맷 변경이 내부 코드까지 흔들게 된다. 그래서 중간에 <strong>번역 계층</strong>이 필요하다.</p>

<h3 id="설계-목표">설계 목표</h3>
<hr />
<ol>
  <li>내부 예외 모델은 순수하게 유지한다.</li>
  <li>외부 응답 계약은 프레젠테이션 계층에서만 조립한다.</li>
  <li>번역 계층은 비즈니스 판단 없이 변환만 수행한다.</li>
</ol>

<h3 id="1-번역-책임은-프레젠테이션-계층에-둔다">1. 번역 책임은 프레젠테이션 계층에 둔다</h3>
<hr />
<p>헥사고날 아키텍처 기준으로 HTTP 응답은 프레젠테이션 어댑터의 책임이다. 따라서 예외를 <code class="language-plaintext highlighter-rouge">ProblemDetail</code>이나 공통 API 에러 포맷으로 바꾸는 일도 프레젠테이션 계층이 맡아야 한다.</p>

<p>즉:</p>

<ul>
  <li>도메인 계층은 예외를 던진다.</li>
  <li>애플리케이션 계층은 흐름을 제어하다가 예외를 던진다.</li>
  <li>프레젠테이션 계층은 그 예외를 HTTP 응답으로 번역한다.</li>
</ul>

<p>그래서 <code class="language-plaintext highlighter-rouge">@RestControllerAdvice</code>는 단순 편의 기능이 아니라, 아키텍처적으로도 자연스러운 번역 지점이다.</p>

<h3 id="2-advisor는-내부-계약을-외부-계약으로-바꾼다">2. Advisor는 내부 계약을 외부 계약으로 바꾼다</h3>
<hr />
<p><code class="language-plaintext highlighter-rouge">Advisor</code>의 역할은 새 규칙을 만드는 것이 아니라, 이미 정의된 내부 계약을 외부 계약으로 <strong>번역</strong>하는 것이다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@RestControllerAdvice</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">GlobalExceptionAdvisor</span> <span class="o">{</span>

    <span class="nd">@ExceptionHandler</span><span class="o">(</span><span class="nc">ApplicationException</span><span class="o">.</span><span class="na">class</span><span class="o">)</span>
    <span class="kd">public</span> <span class="nc">ProblemDetail</span> <span class="nf">handleApplicationException</span><span class="o">(</span><span class="nc">ApplicationException</span> <span class="n">e</span><span class="o">)</span> <span class="o">{</span>
        <span class="nc">HttpStatus</span> <span class="n">status</span> <span class="o">=</span> <span class="n">determineHttpStatus</span><span class="o">(</span><span class="n">e</span><span class="o">.</span><span class="na">getErrorCode</span><span class="o">());</span>

        <span class="nc">ProblemDetail</span> <span class="n">problem</span> <span class="o">=</span> <span class="nc">ProblemDetail</span><span class="o">.</span><span class="na">forStatus</span><span class="o">(</span><span class="n">status</span><span class="o">);</span>
        <span class="n">problem</span><span class="o">.</span><span class="na">setTitle</span><span class="o">(</span><span class="s">"Application error"</span><span class="o">);</span>
        <span class="n">problem</span><span class="o">.</span><span class="na">setDetail</span><span class="o">(</span><span class="n">e</span><span class="o">.</span><span class="na">getMessage</span><span class="o">());</span>
        <span class="n">problem</span><span class="o">.</span><span class="na">setType</span><span class="o">(</span><span class="no">URI</span><span class="o">.</span><span class="na">create</span><span class="o">(</span><span class="s">"https://api.example.com/problems/"</span> <span class="o">+</span> <span class="n">e</span><span class="o">.</span><span class="na">getSpecificCode</span><span class="o">()));</span>
        <span class="n">problem</span><span class="o">.</span><span class="na">setProperty</span><span class="o">(</span><span class="s">"code"</span><span class="o">,</span> <span class="n">e</span><span class="o">.</span><span class="na">getSpecificCode</span><span class="o">());</span>
        <span class="n">problem</span><span class="o">.</span><span class="na">setProperty</span><span class="o">(</span><span class="s">"source"</span><span class="o">,</span> <span class="s">"application"</span><span class="o">);</span>

        <span class="k">return</span> <span class="n">problem</span><span class="o">;</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>핵심은 두 가지다.</p>

<ol>
  <li>상태 코드는 여전히 내부의 <code class="language-plaintext highlighter-rouge">ErrorCode</code>를 기준으로 결정한다.</li>
  <li>클라이언트 식별 코드는 <code class="language-plaintext highlighter-rouge">ProblemSpec</code>이 제공한 값을 응답에 싣는다.</li>
</ol>

<p>즉 <code class="language-plaintext highlighter-rouge">Advisor</code>는 내부 계약을 외부 계약으로 바꿀 뿐, 새로운 비즈니스 판단을 하지 않는다.</p>

<h3 id="3-번역-계층이-하면-안-되는-일">3. 번역 계층이 하면 안 되는 일</h3>
<hr />
<p>번역 계층이 과도하게 똑똑해지면 다시 책임이 섞인다. <code class="language-plaintext highlighter-rouge">Advisor</code>는 아래 일을 하면 안 된다.</p>

<ul>
  <li>도메인 규칙을 새로 판단하는 일</li>
  <li>예외를 보고 비즈니스 흐름을 다시 결정하는 일</li>
  <li>원래 없던 식별 코드를 임의로 만드는 일</li>
</ul>

<p>번역 계층은 이미 만들어진 내부 계약을 받아, 외부 포맷으로 안정적으로 바꾸는 데만 집중해야 한다.</p>

<h3 id="closing">Closing</h3>
<hr />
<p>시리즈를 정리하면 역할은 이렇게 나뉜다.</p>

<ul>
  <li><strong>Internal Exception Model</strong>: 서버 내부에서 실패를 어떻게 모델링할 것인가</li>
  <li><strong>External Error Contract</strong>: 클라이언트에게 어떤 에러 계약을 보여줄 것인가</li>
  <li><strong>Exception Translation Layer</strong>: 그 둘을 어디서, 어떻게 연결할 것인가</li>
</ul>

<p>이렇게 나누면 내부 설계와 외부 계약이 서로를 덜 오염시키고, 각 계층의 책임도 훨씬 선명해진다.</p>]]></content><author><name>HwiRyung Kim</name></author><category term="spring" /><category term="architecture" /><summary type="html"><![CDATA[TOC Overview 왜 번역 계층이 따로 필요한가 설계 목표 1. 번역 책임은 프레젠테이션 계층에 둔다 2. Advisor는 내부 계약을 외부 계약으로 바꾼다 3. 번역 계층이 하면 안 되는 일 Closing]]></summary></entry><entry><title type="html">Modern Beautiful API Response Design: External Error Contract</title><link href="https://hrllk.github.io/modern-beautiful-api-response-design-external-error-contract.html" rel="alternate" type="text/html" title="Modern Beautiful API Response Design: External Error Contract" /><published>2026-03-17T00:00:00+00:00</published><updated>2026-03-17T00:00:00+00:00</updated><id>https://hrllk.github.io/modern-beautiful-api-response-design-external-error-contract</id><content type="html" xml:base="https://hrllk.github.io/modern-beautiful-api-response-design-external-error-contract.html"><![CDATA[<h3 id="toc">TOC</h3>
<hr />
<ol>
  <li><a href="#overview">Overview</a></li>
  <li><a href="#왜-에러-응답도-계약이어야-하는가">왜 에러 응답도 계약이어야 하는가</a></li>
  <li><a href="#설계-목표">설계 목표</a></li>
  <li><a href="#1-내부-예외와-외부-응답은-분리되어야-한다">1. 내부 예외와 외부 응답은 분리되어야 한다</a></li>
  <li><a href="#2-왜-problemdetail인가">2. 왜 ProblemDetail인가</a></li>
  <li><a href="#3-어떤-정보를-응답에-담아야-하는가">3. 어떤 정보를 응답에 담아야 하는가</a></li>
  <li><a href="#4-응답-필드의-책임-분리">4. 응답 필드의 책임 분리</a></li>
  <li><a href="#closing">Closing</a></li>
</ol>

<h3 id="overview">Overview</h3>
<hr />
<p>앞선 글에서는 서버 내부에서 실패를 어떻게 모델링할지 정리했다. 공통 부모 예외, 계층별 예외, <code class="language-plaintext highlighter-rouge">ErrorCode</code>, <code class="language-plaintext highlighter-rouge">ProblemSpec</code>까지 모두 내부 계약의 일부다.</p>

<p>하지만 내부 예외 계약이 잘 설계되어 있어도, 최종적으로 클라이언트가 받는 응답 포맷이 일관되지 않으면 계약은 다시 흔들린다. 그래서 이 단계에서는 예외 자체보다 <strong>응답 번역 규격</strong>이 핵심이 된다.</p>

<p>이 글에서는 내부 예외를 외부 API 에러 응답으로 어떻게 번역할지, 그리고 왜 <code class="language-plaintext highlighter-rouge">ProblemDetail</code>이 이 번역의 중심이 되어야 하는지를 다룬다.</p>

<h3 id="왜-에러-응답도-계약이어야-하는가">왜 에러 응답도 계약이어야 하는가</h3>
<hr />
<p>에러 응답을 즉흥적으로 만들면 보통 아래 문제가 발생한다.</p>

<ol>
  <li>예외마다 JSON 구조가 달라진다.</li>
  <li>HTTP 상태 코드와 본문 의미가 어긋난다.</li>
  <li>클라이언트가 <code class="language-plaintext highlighter-rouge">message</code> 문구에 의존해 분기하게 된다.</li>
  <li>내부 예외 구조가 외부 계약에 그대로 새어 나온다.</li>
</ol>

<p>즉, 내부 설계와 외부 계약 사이에 <strong>번역 계층</strong>이 없으면 응답은 오래 버티지 못한다.</p>

<h3 id="설계-목표">설계 목표</h3>
<hr />
<p>이번 단계의 목표는 명확하다.</p>

<ol>
  <li>내부 예외와 외부 응답 포맷을 분리한다.</li>
  <li>외부 응답은 하나의 표준 구조로 통일한다.</li>
  <li>상태 코드, 식별 코드, 사용자 메시지의 역할을 분리한다.</li>
  <li>번역 책임은 프레젠테이션 계층에서만 담당한다.</li>
</ol>

<h3 id="1-내부-예외와-외부-응답은-분리되어야-한다">1. 내부 예외와 외부 응답은 분리되어야 한다</h3>
<hr />
<p><code class="language-plaintext highlighter-rouge">ApplicationException</code>, <code class="language-plaintext highlighter-rouge">DomainException</code>, <code class="language-plaintext highlighter-rouge">InfrastructureException</code>은 모두 <strong>서버 내부 모델</strong>이다. 이 객체들은 서비스 코드와 아키텍처 책임 분리를 위해 존재한다.</p>

<p>반면 클라이언트가 받아야 하는 것은 서버 내부 클래스 이름이 아니라, <strong>안정적인 외부 계약</strong>이다.<br />
즉 내부에서는:</p>

<ul>
  <li>어떤 계층에서 실패했는가</li>
  <li>어떤 상태 범주인가</li>
  <li>어떤 <code class="language-plaintext highlighter-rouge">ProblemSpec</code>을 사용하는가</li>
</ul>

<p>를 다루고, 외부에서는:</p>

<ul>
  <li>HTTP 상태 코드</li>
  <li>문제 식별자</li>
  <li>사용자 메시지</li>
  <li>요청 문맥</li>
</ul>

<p>을 다뤄야 한다.</p>

<p>이 둘을 같은 객체로 처리하려고 하면 내부 구조가 외부로 새고, 외부 포맷 변경이 내부 설계까지 흔들게 된다.</p>

<h3 id="2-왜-problemdetail인가">2. 왜 ProblemDetail인가</h3>
<hr />
<p>스프링 6부터는 에러 응답을 표현하는 표준 구조로 <code class="language-plaintext highlighter-rouge">ProblemDetail</code>을 제공한다. 이 객체는 HTTP 에러 응답을 일관되게 만들기 위한 최소 골격을 제공한다.</p>

<p>핵심은 단순하다.</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">status</code>: HTTP 상태 코드</li>
  <li><code class="language-plaintext highlighter-rouge">title</code>: 문제의 짧은 이름</li>
  <li><code class="language-plaintext highlighter-rouge">detail</code>: 사람이 읽을 설명</li>
  <li><code class="language-plaintext highlighter-rouge">type</code>: 문제 유형 식별자</li>
  <li><code class="language-plaintext highlighter-rouge">properties</code>: 팀이 추가로 넣고 싶은 확장 필드</li>
</ul>

<p>즉 <code class="language-plaintext highlighter-rouge">ProblemDetail</code>은 “에러 응답의 기본 뼈대”를 제공하고, 우리 시스템은 그 위에 <code class="language-plaintext highlighter-rouge">code</code>, <code class="language-plaintext highlighter-rouge">source</code> 같은 팀 규칙을 얹으면 된다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">ProblemDetail</span> <span class="n">problem</span> <span class="o">=</span> <span class="nc">ProblemDetail</span><span class="o">.</span><span class="na">forStatus</span><span class="o">(</span><span class="nc">HttpStatus</span><span class="o">.</span><span class="na">NOT_FOUND</span><span class="o">);</span>
<span class="n">problem</span><span class="o">.</span><span class="na">setTitle</span><span class="o">(</span><span class="s">"Resource not found"</span><span class="o">);</span>
<span class="n">problem</span><span class="o">.</span><span class="na">setDetail</span><span class="o">(</span><span class="s">"User not found"</span><span class="o">);</span>
<span class="n">problem</span><span class="o">.</span><span class="na">setType</span><span class="o">(</span><span class="no">URI</span><span class="o">.</span><span class="na">create</span><span class="o">(</span><span class="s">"https://api.example.com/problems/user-not-found"</span><span class="o">));</span>
<span class="n">problem</span><span class="o">.</span><span class="na">setProperty</span><span class="o">(</span><span class="s">"code"</span><span class="o">,</span> <span class="s">"USER-404"</span><span class="o">);</span>
<span class="n">problem</span><span class="o">.</span><span class="na">setProperty</span><span class="o">(</span><span class="s">"source"</span><span class="o">,</span> <span class="s">"application"</span><span class="o">);</span>
</code></pre></div></div>

<h3 id="3-어떤-정보를-응답에-담아야-하는가">3. 어떤 정보를 응답에 담아야 하는가</h3>
<hr />
<p>여기서 중요한 것은 “많이 담는 것”이 아니라, <strong>각 필드가 무엇을 책임지는지 분리하는 것</strong>이다.</p>

<ul>
  <li><strong>HTTP status</strong>: 전송 계층의 의미다. 요청이 실패했는지, 권한 문제인지, 서버 문제인지 표현한다.</li>
  <li><strong><code class="language-plaintext highlighter-rouge">type</code></strong>: 문제 유형의 안정적인 식별자다.</li>
  <li><strong><code class="language-plaintext highlighter-rouge">code</code></strong>: 프론트엔드나 운영 도구가 분기하기 쉬운 팀 내부 규격 코드다.</li>
  <li><strong><code class="language-plaintext highlighter-rouge">detail</code></strong>: 사람이 읽을 수 있는 설명이다.</li>
  <li><strong><code class="language-plaintext highlighter-rouge">source</code></strong>: 선택적이다. 어느 계층에서 번역되었는지 드러내고 싶을 때만 둔다.</li>
</ul>

<p>즉 같은 “에러”라도 역할은 다르다.</p>

<ul>
  <li>상태 코드는 HTTP를 위한 것</li>
  <li>식별 코드는 클라이언트 분기를 위한 것</li>
  <li>메시지는 사람을 위한 것</li>
</ul>

<p>이 셋을 하나의 필드로 뭉개면 다시 문자열 파싱 구조로 되돌아간다.</p>

<h3 id="4-응답-필드의-책임-분리">4. 응답 필드의 책임 분리</h3>
<hr />
<p>앞선 두 글과 이번 글을 합치면 역할은 이렇게 정리된다.</p>

<ul>
  <li><strong>Exception Hierarchy</strong>: 실패의 출처와 책임을 분리한다.</li>
  <li><strong><code class="language-plaintext highlighter-rouge">ErrorCode</code></strong>: 각 계층이 가질 수 있는 본질적인 상태 범주를 표현한다.</li>
  <li><strong><code class="language-plaintext highlighter-rouge">ProblemSpec</code></strong>: 외부에 노출할 식별 코드와 메시지 키를 관리한다.</li>
  <li><strong><code class="language-plaintext highlighter-rouge">ProblemDetail</code></strong>: 외부 에러 응답 계약의 기본 뼈대를 제공한다.</li>
</ul>

<p>즉 내부에서는 아키텍처를 지키고, 외부에서는 표준 응답 포맷을 지키는 구조다. 이 둘을 하나로 합치지 않고, 번역 지점을 둔 것이 핵심이다.</p>

<h3 id="closing">Closing</h3>
<hr />
<p>예외 계층과 <code class="language-plaintext highlighter-rouge">ErrorCode</code>, <code class="language-plaintext highlighter-rouge">ProblemSpec</code>이 내부 계약의 뼈대라면, <code class="language-plaintext highlighter-rouge">ProblemDetail</code>은 그 계약을 외부 세계로 내보내는 표준 포맷이다. 내부 설계와 외부 응답을 분리해 두면, 서버는 더 안정적으로 진화하고 클라이언트는 더 예측 가능한 계약 위에서 동작할 수 있다.</p>

<p>다음 글에서는 이 외부 계약을 실제로 <a href="/modern-beautiful-api-response-design-exception-translation-layer.html">어디서, 어떻게 번역해야 하는지</a> 다룬다. 즉 <code class="language-plaintext highlighter-rouge">@RestControllerAdvice</code>와 번역 계층의 위치를 정리한다.</p>]]></content><author><name>HwiRyung Kim</name></author><category term="spring" /><category term="architecture" /><summary type="html"><![CDATA[TOC Overview 왜 에러 응답도 계약이어야 하는가 설계 목표 1. 내부 예외와 외부 응답은 분리되어야 한다 2. 왜 ProblemDetail인가 3. 어떤 정보를 응답에 담아야 하는가 4. 응답 필드의 책임 분리 Closing]]></summary></entry><entry><title type="html">Modern Beautiful API Response Design: Internal Exception Model</title><link href="https://hrllk.github.io/modern-beautiful-api-response-design-internal-exception-model.html" rel="alternate" type="text/html" title="Modern Beautiful API Response Design: Internal Exception Model" /><published>2026-03-16T00:00:00+00:00</published><updated>2026-03-16T00:00:00+00:00</updated><id>https://hrllk.github.io/modern-beautiful-api-response-design-internal-exception-model</id><content type="html" xml:base="https://hrllk.github.io/modern-beautiful-api-response-design-internal-exception-model.html"><![CDATA[<h3 id="toc">TOC</h3>
<hr />
<ol>
  <li><a href="#overview">Overview</a></li>
  <li><a href="#평면적인-예외-정의의-문제">평면적인 예외 정의의 문제</a></li>
  <li><a href="#설계-목표">설계 목표</a></li>
  <li><a href="#1-공통-부모-예외">1. 공통 부모 예외</a></li>
  <li><a href="#2-계층별-예외-분리">2. 계층별 예외 분리</a></li>
  <li><a href="#3-계층별-베이스-예외">3. 계층별 베이스 예외</a></li>
  <li><a href="#4-번역-책임의-위치">4. 번역 책임의 위치</a></li>
  <li><a href="#5-errorcode는-분류다">5. ErrorCode는 분류다</a></li>
  <li><a href="#6-problemspec은-공개-명세다">6. ProblemSpec은 공개 명세다</a></li>
  <li><a href="#7-커스텀-예외와의-결합">7. 커스텀 예외와의 결합</a></li>
  <li><a href="#closing">Closing</a></li>
</ol>

<h3 id="overview">Overview</h3>
<hr />
<p>예외를 설계하지 않은 시스템은 결국 문자열에 의존하게 된다.</p>

<p>처음에는 표준 예외만으로도 기능 구현이 가능해 보이지만, 서비스가 커질수록 문제가 드러난다. 예외 타입과 메시지 형식이 제각각이며, 비즈니스 오류인지 인프라 장애인지 구분하기 어렵다. 클라이언트는 문장 파싱에 가까운 분기 처리를 하게 되고, 서버는 로그를 봐도 무엇이 의도된 실패인지 판단하기 어렵다.</p>

<p>예외에도 <strong>족보</strong>가 필요하다. 예외를 계층화하면 실패의 출처와 책임을 더 명확하게 통제할 수 있다.</p>

<p>이 글의 범위는 <strong>서버 내부 예외 모델</strong>까지다. 즉, 공통 부모 예외, 계층별 예외, <code class="language-plaintext highlighter-rouge">ErrorCode</code>, <code class="language-plaintext highlighter-rouge">ProblemSpec</code>이 내부에서 어떤 책임을 가져야 하는지까지 설명한다.</p>

<h3 id="평면적인-예외-정의의-문제">평면적인 예외 정의의 문제</h3>
<hr />
<p>예외를 즉흥적으로 추가하면 다음과 같은 문제가 발생한다.</p>

<ol>
  <li>동일한 실패 상황에서도 예외 타입과 메시지가 파편화된다.</li>
  <li>예외가 발생한 계층의 역할이 모호해진다.</li>
  <li>로그에서 의도된 비즈니스 실패와 실제 시스템 장애를 분리하기 어렵다.</li>
  <li>예외 번역 책임이 뒤섞여 응답 계약도 흔들린다.</li>
</ol>

<h3 id="설계-목표">설계 목표</h3>
<hr />
<p>계층형 예외 설계의 목표는 명확하다.</p>

<ol>
  <li><strong>모든 커스텀 예외가 공통 부모를 따른다.</strong></li>
  <li><strong>각 계층은 자기 책임에 맞는 예외만 던진다.</strong></li>
  <li><strong>예외 번역 책임은 프레젠테이션 계층으로 밀어낸다.</strong></li>
</ol>

<h3 id="1-공통-부모-예외">1. 공통 부모 예외</h3>
<hr />
<p>모든 예외의 근간이 되는 <code class="language-plaintext highlighter-rouge">BaseException</code>은 최소한의 공통 계약만 가진다. 운영 코드에서는 <code class="language-plaintext highlighter-rouge">JgitkinsException</code>처럼 “명시 메시지가 없으면 기본 메시지로 fallback”하는 생성자 체계를 두는 편이 실용적이다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">interface</span> <span class="nc">ErrorCode</span> <span class="o">{</span>
    <span class="nc">String</span> <span class="nf">getCode</span><span class="o">();</span>
    <span class="nc">String</span> <span class="nf">getDefaultMessage</span><span class="o">();</span>
<span class="o">}</span>

<span class="kd">public</span> <span class="kd">abstract</span> <span class="kd">class</span> <span class="nc">BaseException</span> <span class="kd">extends</span> <span class="nc">RuntimeException</span> <span class="o">{</span>
    <span class="kd">private</span> <span class="kd">final</span> <span class="nc">ErrorCode</span> <span class="n">errorCode</span><span class="o">;</span>

    <span class="kd">protected</span> <span class="nf">BaseException</span><span class="o">(</span><span class="nc">ErrorCode</span> <span class="n">errorCode</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">this</span><span class="o">(</span><span class="n">errorCode</span><span class="o">,</span> <span class="kc">null</span><span class="o">,</span> <span class="kc">null</span><span class="o">);</span>
    <span class="o">}</span>

    <span class="kd">protected</span> <span class="nf">BaseException</span><span class="o">(</span><span class="nc">ErrorCode</span> <span class="n">errorCode</span><span class="o">,</span> <span class="nc">String</span> <span class="n">message</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">this</span><span class="o">(</span><span class="n">errorCode</span><span class="o">,</span> <span class="n">message</span><span class="o">,</span> <span class="kc">null</span><span class="o">);</span>
    <span class="o">}</span>

    <span class="kd">protected</span> <span class="nf">BaseException</span><span class="o">(</span><span class="nc">ErrorCode</span> <span class="n">errorCode</span><span class="o">,</span> <span class="nc">String</span> <span class="n">message</span><span class="o">,</span> <span class="nc">Throwable</span> <span class="n">cause</span><span class="o">)</span> <span class="o">{</span>
        <span class="kd">super</span><span class="o">((</span><span class="n">message</span> <span class="o">==</span> <span class="kc">null</span> <span class="o">||</span> <span class="n">message</span><span class="o">.</span><span class="na">isBlank</span><span class="o">())</span>
                <span class="o">?</span> <span class="n">errorCode</span><span class="o">.</span><span class="na">getDefaultMessage</span><span class="o">()</span>
                <span class="o">:</span> <span class="n">message</span><span class="o">,</span> <span class="n">cause</span><span class="o">);</span>
        <span class="k">this</span><span class="o">.</span><span class="na">errorCode</span> <span class="o">=</span> <span class="n">errorCode</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="kd">public</span> <span class="nc">ErrorCode</span> <span class="nf">getErrorCode</span><span class="o">()</span> <span class="o">{</span>
        <span class="k">return</span> <span class="n">errorCode</span><span class="o">;</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<h3 id="2-계층별-예외-분리">2. 계층별 예외 분리</h3>
<hr />

<ul>
  <li><strong><code class="language-plaintext highlighter-rouge">ApplicationException</code></strong>: 흐름 제어, 조회 결과, 권한</li>
  <li><strong><code class="language-plaintext highlighter-rouge">DomainException</code></strong>: 비즈니스 규칙, 상태 전이, 정책 위반</li>
  <li><strong><code class="language-plaintext highlighter-rouge">InfrastructureException</code></strong>: DB, 파일시스템, 외부 라이브러리 실패</li>
</ul>

<p>이렇게 나누면 실패 원인을 “무슨 일이 일어났는가”뿐 아니라 “어느 계층 책임인가”까지 함께 표현할 수 있다.</p>

<h3 id="3-계층별-베이스-예외">3. 계층별 베이스 예외</h3>
<hr />
<p>이제 <code class="language-plaintext highlighter-rouge">BaseException</code>을 상속받아 계층별 최상위 예외를 만든다. 각 계층은 자기 계층의 명세만 받도록 제한해야 한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">abstract</span> <span class="kd">class</span> <span class="nc">DomainException</span> <span class="kd">extends</span> <span class="nc">BaseException</span> <span class="o">{</span>
    <span class="kd">private</span> <span class="kd">final</span> <span class="nc">DomainProblemSpec</span> <span class="n">problemSpec</span><span class="o">;</span>

    <span class="kd">protected</span> <span class="nf">DomainException</span><span class="o">(</span><span class="nc">DomainProblemSpec</span> <span class="n">problemSpec</span><span class="o">)</span> <span class="o">{</span>
        <span class="kd">super</span><span class="o">(</span><span class="n">problemSpec</span><span class="o">.</span><span class="na">getErrorCode</span><span class="o">());</span>
        <span class="k">this</span><span class="o">.</span><span class="na">problemSpec</span> <span class="o">=</span> <span class="n">problemSpec</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="kd">public</span> <span class="nc">String</span> <span class="nf">getSpecificCode</span><span class="o">()</span> <span class="o">{</span>
        <span class="k">return</span> <span class="n">problemSpec</span><span class="o">.</span><span class="na">getCode</span><span class="o">();</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">abstract</span> <span class="kd">class</span> <span class="nc">ApplicationException</span> <span class="kd">extends</span> <span class="nc">BaseException</span> <span class="o">{</span>
    <span class="kd">private</span> <span class="kd">final</span> <span class="nc">ApplicationProblemSpec</span> <span class="n">problemSpec</span><span class="o">;</span>

    <span class="kd">protected</span> <span class="nf">ApplicationException</span><span class="o">(</span><span class="nc">ApplicationProblemSpec</span> <span class="n">problemSpec</span><span class="o">)</span> <span class="o">{</span>
        <span class="kd">super</span><span class="o">(</span><span class="n">problemSpec</span><span class="o">.</span><span class="na">getErrorCode</span><span class="o">());</span>
        <span class="k">this</span><span class="o">.</span><span class="na">problemSpec</span> <span class="o">=</span> <span class="n">problemSpec</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="kd">public</span> <span class="nc">String</span> <span class="nf">getSpecificCode</span><span class="o">()</span> <span class="o">{</span>
        <span class="k">return</span> <span class="n">problemSpec</span><span class="o">.</span><span class="na">getCode</span><span class="o">();</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">abstract</span> <span class="kd">class</span> <span class="nc">InfrastructureException</span> <span class="kd">extends</span> <span class="nc">BaseException</span> <span class="o">{</span>
    <span class="kd">private</span> <span class="kd">final</span> <span class="nc">InfrastructureProblemSpec</span> <span class="n">problemSpec</span><span class="o">;</span>

    <span class="kd">protected</span> <span class="nf">InfrastructureException</span><span class="o">(</span><span class="nc">InfrastructureProblemSpec</span> <span class="n">problemSpec</span><span class="o">,</span> <span class="nc">Throwable</span> <span class="n">cause</span><span class="o">)</span> <span class="o">{</span>
        <span class="kd">super</span><span class="o">(</span><span class="n">problemSpec</span><span class="o">.</span><span class="na">getErrorCode</span><span class="o">(),</span> <span class="kc">null</span><span class="o">,</span> <span class="n">cause</span><span class="o">);</span>
        <span class="k">this</span><span class="o">.</span><span class="na">problemSpec</span> <span class="o">=</span> <span class="n">problemSpec</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="kd">public</span> <span class="nc">String</span> <span class="nf">getSpecificCode</span><span class="o">()</span> <span class="o">{</span>
        <span class="k">return</span> <span class="n">problemSpec</span><span class="o">.</span><span class="na">getCode</span><span class="o">();</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>이 구조는 도메인 로직에서 인프라 예외를 던지는 식의 계층 오염을 컴파일 타임에 줄여 준다.</p>

<h3 id="4-번역-책임의-위치">4. 번역 책임의 위치</h3>
<hr />
<p>비즈니스 로직은 순수하게 예외만 던지고, 이를 실제 HTTP 응답으로 바꾸는 책임은 <code class="language-plaintext highlighter-rouge">@RestControllerAdvice</code>가 담당한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@RestControllerAdvice</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">GlobalExceptionAdvisor</span> <span class="o">{</span>

    <span class="nd">@ExceptionHandler</span><span class="o">(</span><span class="nc">ApplicationException</span><span class="o">.</span><span class="na">class</span><span class="o">)</span>
    <span class="kd">public</span> <span class="nc">ResponseEntity</span><span class="o">&lt;</span><span class="nc">CommonResponse</span><span class="o">&lt;?&gt;&gt;</span> <span class="n">handleApplicationException</span><span class="o">(</span><span class="nc">ApplicationException</span> <span class="n">e</span><span class="o">)</span> <span class="o">{</span>
        <span class="nc">HttpStatus</span> <span class="n">status</span> <span class="o">=</span> <span class="n">determineHttpStatus</span><span class="o">(</span><span class="n">e</span><span class="o">.</span><span class="na">getErrorCode</span><span class="o">());</span>
        <span class="k">return</span> <span class="nc">ResponseEntity</span><span class="o">.</span><span class="na">status</span><span class="o">(</span><span class="n">status</span><span class="o">)</span>
                <span class="o">.</span><span class="na">body</span><span class="o">(</span><span class="nc">CommonResponse</span><span class="o">.</span><span class="na">fail</span><span class="o">(</span><span class="n">e</span><span class="o">.</span><span class="na">getSpecificCode</span><span class="o">(),</span> <span class="n">e</span><span class="o">.</span><span class="na">getMessage</span><span class="o">()));</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>이 시점까지는 아직 <strong>서버 내부 모델</strong>이다. 실제 외부 응답 포맷 자체는 다음 글에서 다룬다.</p>

<h3 id="5-errorcode는-분류다">5. ErrorCode는 분류다</h3>
<hr />
<p>예외 계층만 나눈다고 계약이 완성되지는 않는다. 각 계층이 가질 수 있는 <strong>상태 범주</strong>도 정리해야 한다.</p>

<p><code class="language-plaintext highlighter-rouge">ErrorCode</code>의 구현체인 enum에는 <code class="language-plaintext highlighter-rouge">"USER-404"</code> 같은 구체 코드를 넣지 않는다. 대신 각 계층이 가질 수 있는 <strong>본질적인 실패 범주</strong>만 둔다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">enum</span> <span class="nc">DomainErrorCode</span> <span class="kd">implements</span> <span class="nc">ErrorCode</span> <span class="o">{</span>
    <span class="no">RULE_VIOLATION</span><span class="o">(</span><span class="s">"RULE_VIOLATION"</span><span class="o">,</span> <span class="s">"Domain rule violation"</span><span class="o">),</span>
    <span class="no">INVALID_STATE</span><span class="o">(</span><span class="s">"INVALID_STATE"</span><span class="o">,</span> <span class="s">"Domain state transition is invalid"</span><span class="o">),</span>
    <span class="no">POLICY_VIOLATION</span><span class="o">(</span><span class="s">"POLICY_VIOLATION"</span><span class="o">,</span> <span class="s">"Domain policy violation"</span><span class="o">);</span>
<span class="o">}</span>
</code></pre></div></div>

<p>예를 들면:</p>

<ul>
  <li><strong><code class="language-plaintext highlighter-rouge">ApplicationErrorCode</code></strong>: 흐름 제어, 인증/인가, 조회 결과</li>
  <li><strong><code class="language-plaintext highlighter-rouge">DomainErrorCode</code></strong>: 비즈니스 규칙, 상태 전이, 정책 위반</li>
  <li><strong><code class="language-plaintext highlighter-rouge">InfraErrorCode</code></strong>: DB, 파일시스템, 외부 라이브러리 실패</li>
</ul>

<h3 id="6-problemspec은-공개-명세다">6. ProblemSpec은 공개 명세다</h3>
<hr />
<p><code class="language-plaintext highlighter-rouge">ProblemSpec</code>이 필요한 이유는 단순히 <code class="language-plaintext highlighter-rouge">"USER-404"</code> 같은 문자열을 한곳에 모으기 위해서가 아니다. 더 중요한 목적은 <strong>계층별 상태 범주와 예외별 식별자를 분리</strong>하는 데 있다.</p>

<p><code class="language-plaintext highlighter-rouge">ApplicationErrorCode</code>, <code class="language-plaintext highlighter-rouge">DomainErrorCode</code>, <code class="language-plaintext highlighter-rouge">InfrastructureErrorCode</code>는 각 계층이 가질 수 있는 <strong>본질적인 실패 범주</strong>만 표현해야 한다. 반대로 클라이언트가 분기 처리에 사용할 <code class="language-plaintext highlighter-rouge">"USER-404"</code>, <code class="language-plaintext highlighter-rouge">"REPO-409"</code> 같은 식별자는 훨씬 더 구체적이고 자주 늘어나거나 바뀔 수 있다. 이 식별자까지 <code class="language-plaintext highlighter-rouge">ErrorCode</code> enum에 넣어버리면, 계층 enum이 범주 표현이 아니라 개별 사례 목록으로 비대해진다.</p>

<p>그래서 <code class="language-plaintext highlighter-rouge">ProblemSpec</code>은 <strong>계층 enum을 건드리지 않고도</strong>, 예외별 식별 코드, 기본 메시지, 메시지 키 같은 구체 명세를 중앙에서 관리하게 해 준다. 즉, <code class="language-plaintext highlighter-rouge">ErrorCode</code>는 <strong>분류</strong>, <code class="language-plaintext highlighter-rouge">ProblemSpec</code>은 <strong>공개 명세</strong>다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">interface</span> <span class="nc">ProblemSpec</span><span class="o">&lt;</span><span class="no">T</span> <span class="kd">extends</span> <span class="nc">ErrorCode</span><span class="o">&gt;</span> <span class="o">{</span>
    <span class="no">T</span> <span class="nf">getErrorCode</span><span class="o">();</span>
    <span class="nc">String</span> <span class="nf">getCode</span><span class="o">();</span>
    <span class="nc">String</span> <span class="nf">getDefaultMessage</span><span class="o">();</span>
    <span class="nc">String</span> <span class="nf">getMessageKey</span><span class="o">();</span>
<span class="o">}</span>
</code></pre></div></div>

<p>실무에서는 공통 <code class="language-plaintext highlighter-rouge">ProblemSpec</code> 인터페이스 위에 계층별 카탈로그 enum을 두는 방식이 가장 단순하다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">enum</span> <span class="nc">ApplicationProblemSpec</span> <span class="kd">implements</span> <span class="nc">ProblemSpec</span><span class="o">&lt;</span><span class="nc">ApplicationErrorCode</span><span class="o">&gt;</span> <span class="o">{</span>
    <span class="no">USER_NOT_FOUND</span><span class="o">(</span><span class="nc">ApplicationErrorCode</span><span class="o">.</span><span class="na">NOT_FOUND</span><span class="o">,</span> <span class="s">"USER-404"</span><span class="o">,</span> <span class="s">"User not found"</span><span class="o">,</span> <span class="s">"user.notFound"</span><span class="o">);</span>
    <span class="c1">// 필드, 생성자, getter 생략</span>
<span class="o">}</span>

<span class="kd">public</span> <span class="kd">enum</span> <span class="nc">DomainProblemSpec</span> <span class="kd">implements</span> <span class="nc">ProblemSpec</span><span class="o">&lt;</span><span class="nc">DomainErrorCode</span><span class="o">&gt;</span> <span class="o">{</span>
    <span class="no">INVALID_ORDER_STATE</span><span class="o">(</span><span class="nc">DomainErrorCode</span><span class="o">.</span><span class="na">INVALID_STATE</span><span class="o">,</span> <span class="s">"ORDER-409"</span><span class="o">,</span> <span class="s">"Order state is invalid"</span><span class="o">,</span> <span class="s">"order.invalidState"</span><span class="o">);</span>
    <span class="c1">// 필드, 생성자, getter 생략</span>
<span class="o">}</span>
</code></pre></div></div>

<h3 id="7-커스텀-예외와의-결합">7. 커스텀 예외와의 결합</h3>
<hr />
<p>실제 비즈니스 로직에서는 상태(Enum)와 코드(String)를 예외 클래스 내부 문자열이 아니라, 계층별 <code class="language-plaintext highlighter-rouge">ProblemSpec</code> 카탈로그에서 결합한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">class</span> <span class="nc">UserNotFoundException</span> <span class="kd">extends</span> <span class="nc">ApplicationException</span> <span class="o">{</span>
    <span class="kd">public</span> <span class="nf">UserNotFoundException</span><span class="o">()</span> <span class="o">{</span>
        <span class="kd">super</span><span class="o">(</span><span class="nc">ApplicationProblemSpec</span><span class="o">.</span><span class="na">USER_NOT_FOUND</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>

<span class="kd">public</span> <span class="kd">class</span> <span class="nc">InvalidOrderStateException</span> <span class="kd">extends</span> <span class="nc">DomainException</span> <span class="o">{</span>
    <span class="kd">public</span> <span class="nf">InvalidOrderStateException</span><span class="o">()</span> <span class="o">{</span>
        <span class="kd">super</span><span class="o">(</span><span class="nc">DomainProblemSpec</span><span class="o">.</span><span class="na">INVALID_ORDER_STATE</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>이렇게 하면 예외 클래스는 “무슨 상황인가”만 표현하고, 코드 체계의 유일성과 명세 소유권은 <code class="language-plaintext highlighter-rouge">ProblemSpec</code> 카탈로그에 모을 수 있다.</p>

<h3 id="closing">Closing</h3>
<hr />
<p>예외 계층, <code class="language-plaintext highlighter-rouge">ErrorCode</code>, <code class="language-plaintext highlighter-rouge">ProblemSpec</code>을 함께 정리하면 내부 예외 모델의 뼈대가 완성된다. 이 글의 목적은 어디까지나 <strong>서버 내부에서 실패를 어떻게 모델링할 것인가</strong>를 정리하는 데 있다.</p>

<p>다음 글에서는 이 내부 예외 모델을 바탕으로 <a href="/modern-beautiful-api-response-design-external-error-contract.html"><strong>외부 에러 응답 계약</strong></a>을 어떻게 설계할지 다룬다.</p>]]></content><author><name>HwiRyung Kim</name></author><category term="spring" /><category term="architecture" /><summary type="html"><![CDATA[TOC Overview 평면적인 예외 정의의 문제 설계 목표 1. 공통 부모 예외 2. 계층별 예외 분리 3. 계층별 베이스 예외 4. 번역 책임의 위치 5. ErrorCode는 분류다 6. ProblemSpec은 공개 명세다 7. 커스텀 예외와의 결합 Closing]]></summary></entry><entry><title type="html">Modern Beautiful API Response Design: Prologue</title><link href="https://hrllk.github.io/modern-beautiful-api-response-design-prologue.html" rel="alternate" type="text/html" title="Modern Beautiful API Response Design: Prologue" /><published>2026-03-15T00:00:00+00:00</published><updated>2026-03-15T00:00:00+00:00</updated><id>https://hrllk.github.io/modern-beautiful-api-response-design-prologue</id><content type="html" xml:base="https://hrllk.github.io/modern-beautiful-api-response-design-prologue.html"><![CDATA[<h3 id="toc">TOC</h3>
<hr />
<ol>
  <li><a href="#overview">Overview</a></li>
  <li><a href="#목표">목표</a></li>
  <li><a href="#연재-계획">연재 계획</a></li>
  <li><a href="#약속-our-principle">약속 (Our Principle)</a></li>
  <li><a href="#next-step">Next Step</a></li>
</ol>

<h3 id="overview">Overview</h3>
<hr />
<p>API 응답 시스템은 보통 프로젝트 후반에 뒤늦게 손보게 된다. 초반에는 기능 구현이 우선이라 예외 메시지, HTTP 상태 코드, 에러 JSON 포맷이 제각각으로 흩어지기 쉽다.</p>

<p>하지만 시스템이 커질수록 이 파편화는 곧 협업 비용이 된다. 프론트엔드는 매번 다른 에러 형태에 맞춰 분기해야 하고, 백엔드는 로그에서 의도된 비즈니스 실패와 실제 장애를 구분하느라 시간을 낭비한다.</p>

<p>견고하고 예측 가능한 API를 만들려면 세 가지를 함께 설계해야 한다.</p>

<ol>
  <li>예외를 어떤 기준으로 분류할 것인가</li>
  <li>그 예외를 어떤 공통 응답 포맷으로 번역할 것인가</li>
  <li>그 번역 책임을 아키텍처 어디에 둘 것인가</li>
</ol>

<p>이 시리즈는 바로 이 세 질문에 답하기 위한 글이다. 첫 글에서는 전체 그림과 연재 순서를 정리하고, 이어지는 글들에서 예외 계약, 응답 포맷, 예외 핸들러의 위치를 순서대로 다룬다.</p>

<h3 id="목표">목표</h3>
<hr />
<p>이 시리즈가 해결하려는 핵심 목표는 아래와 같다.</p>

<ul>
  <li><strong>예외 계약 정리:</strong> 예외를 계층별로 분류하고, 상태 범주와 식별 코드를 분리한다.</li>
  <li><strong>응답 포맷 통일:</strong> 내부 예외를 일관된 외부 API 응답으로 번역한다.</li>
  <li><strong>아키텍처 정합성 확보:</strong> 예외 번역 책임을 헥사고날 아키텍처에 맞는 위치에 둔다.</li>
</ul>

<h3 id="연재-계획">연재 계획</h3>
<hr />
<ol>
  <li><strong>[프롤로그] 왜 API 응답 설계를 별도로 다뤄야 하는가</strong><br />
<a href="/modern-beautiful-api-response-design-prologue.html"><em>현재 글 보기</em></a></li>
  <li><strong>[1편] 서버 내부에서 실패를 어떻게 모델링할 것인가: Internal Exception Model</strong><br />
<a href="/modern-beautiful-api-response-design-internal-exception-model.html">modern-beautiful-api-response-design-internal-exception-model.html</a></li>
  <li><strong>[2편] 클라이언트에게 어떤 에러 계약을 보여줄 것인가: External Error Contract</strong><br />
<a href="/2026-03-17-modern-beautiful-api-response-design-external-error-contract.html">2026-03-17-modern-beautiful-api-response-design-external-error-contract.html</a></li>
  <li><strong>[3편] 내부 모델과 외부 계약을 어디서 연결할 것인가: Exception Translation Layer</strong><br />
<a href="/2026-03-18-modern-beautiful-api-response-design-exception-translation-layer.html">2026-03-18-modern-beautiful-api-response-design-exception-translation-layer.html</a></li>
</ol>

<!--
5. **[Draft] Exception Scaffolding Draft**  
   [/2026/03/19/modern-beautiful-api-response-design-exception-scaffolding-draft.html](/2026/03/19/modern-beautiful-api-response-design-exception-scaffolding-draft.html)
-->

<h3 id="약속-our-principle">약속 (Our Principle)</h3>
<hr />
<ul>
  <li><strong>분리:</strong> 예외 분류, 응답 포맷, 번역 책임을 한 글에 모두 밀어 넣지 않는다.</li>
  <li><strong>명확성:</strong> 클라이언트와 서버가 모두 이해할 수 있는 안정적인 계약을 지향한다.</li>
  <li><strong>정합성:</strong> 단순한 코드 스타일이 아니라 아키텍처 책임 분리까지 함께 검토한다.</li>
</ul>

<h3 id="next-step">Next Step</h3>
<hr />

<p>다음 포스트에서는 <a href="/modern-beautiful-api-response-design-internal-exception-model.html">Modern Beautiful API Response Design: Internal Exception Model</a> 을 다룬다.
먼저 서버 내부에서 실패를 어떻게 모델링할지부터 정리한다.</p>]]></content><author><name>HwiRyung Kim</name></author><category term="architecture" /><summary type="html"><![CDATA[TOC Overview 목표 연재 계획 약속 (Our Principle) Next Step]]></summary></entry><entry><title type="html">when should switch working clone to bare clone</title><link href="https://hrllk.github.io/when-should-switch-working-clone-to-bare-clone.html" rel="alternate" type="text/html" title="when should switch working clone to bare clone" /><published>2026-03-13T00:00:00+00:00</published><updated>2026-03-13T00:00:00+00:00</updated><id>https://hrllk.github.io/when-should-switch-working-clone-to-bare-clone</id><content type="html" xml:base="https://hrllk.github.io/when-should-switch-working-clone-to-bare-clone.html"><![CDATA[<h3 id="overview">Overview</h3>
<hr />
<p>Git 저장소를 다룰 때 우리는 습관적으로 <code class="language-plaintext highlighter-rouge">git clone</code> 결과를 작업 디렉터리까지 포함한 형태로 받아온다.
이 방식은 익숙하고 직관적이다.
클론이 끝나면 곧바로 소스 파일을 열어볼 수 있고, 수정도 가능하며, 빌드나 테스트도 바로 수행할 수 있다.</p>

<p>그런데 모든 작업이 정말 working tree를 필요로 하는 것은 아니다.
어떤 작업은 실제 소스 파일을 한 줄도 읽지 않고, 오직 Git metadata만으로 끝난다.
예를 들어 변경 이력 비교, ref 조회, fetch, commit graph 탐색 같은 작업은 <code class="language-plaintext highlighter-rouge">.git</code> 내부 정보만으로도 충분히 수행할 수 있다.</p>

<p>이 지점에서 자연스럽게 질문이 생긴다.</p>

<blockquote>
  <p>“working clone이 아니라 bare clone으로 바꾸면 비용을 더 줄일 수 있지 않을까?”</p>
</blockquote>

<p>이번 글은 이 질문에 대해 정리한 초안이다.
핵심은 단순히 <code class="language-plaintext highlighter-rouge">bare clone이 더 빠르다</code> 라는 결론을 내리는 것이 아니라, 어떤 경우에는 bare clone이 맞고 어떤 경우에는 여전히 working clone이 필요하다는 점을 경계선 중심으로 설명하는 데 있다.</p>

<h3 id="차이점">차이점?</h3>
<hr />
<blockquote>
  <p>Working Clone과 Bare Clone은 무엇이 다를까?</p>
</blockquote>

<p><code class="language-plaintext highlighter-rouge">working clone</code> 은 우리가 가장 익숙하게 사용하는 형태의 클론이다.
Git object database와 refs 정보가 <code class="language-plaintext highlighter-rouge">.git</code> 디렉터리 안에 저장되고, 동시에 현재 브랜치 기준의 실제 파일들도 working tree에 풀린다.
즉, Git 내부 데이터와 사람이 보는 소스 트리가 함께 생성된다.</p>

<p>반면 <code class="language-plaintext highlighter-rouge">bare clone</code> 은 working tree가 없다.
저장소 자체가 곧 Git metadata 저장소이며, 일반적으로 ref, object, pack file 중심의 구조만 남는다.
그래서 bare 저장소를 열어보면 Java 파일, YAML 파일, Gradle 파일이 보이지 않는다.
대신 <code class="language-plaintext highlighter-rouge">HEAD</code>, <code class="language-plaintext highlighter-rouge">config</code>, <code class="language-plaintext highlighter-rouge">packed-refs</code>, <code class="language-plaintext highlighter-rouge">objects/pack/*.pack</code>, <code class="language-plaintext highlighter-rouge">objects/pack/*.idx</code> 같은 Git 내부 파일 위주로 구성된다.</p>

<p>이 차이는 단순한 저장 형태의 차이가 아니라 비용 구조의 차이이기도 하다.
working clone은 fetch 이후 checkout 단계까지 수행해야 하므로 파일 생성, 디렉터리 생성, metadata 기록, 권한 반영 등 추가 파일시스템 작업이 따라온다.
bare clone은 이 checkout 단계를 생략하므로 생성 파일 수와 디스크 사용량을 눈에 띄게 줄일 수 있다.</p>

<h3 id="why">Why?</h3>
<hr />
<blockquote>
  <p>왜 Bare Clone이 더 가벼워 보일까?</p>
</blockquote>

<p>실제로 성능 테스트를 수행해보면 working clone과 bare clone의 결과는 꽤 다르게 보인다.
예를 들어 한 테스트에서는 working clone 결과가 약 <code class="language-plaintext highlighter-rouge">2781</code>개의 일반 파일과 <code class="language-plaintext highlighter-rouge">160 MiB</code> 수준의 디렉터리 크기로 측정되었고, bare clone 결과는 약 <code class="language-plaintext highlighter-rouge">6</code>개의 일반 파일과 <code class="language-plaintext highlighter-rouge">66 MiB</code> 수준으로 측정되었다.</p>

<p>이 수치만 보면 bare clone이 압도적으로 가볍다고 느껴진다.
실제로도 디스크 점유량과 생성 파일 수 관점에서는 그 해석이 크게 틀리지 않다.
다만 여기서 오해하면 안 되는 부분이 있다.</p>

<p>bare 저장소의 파일 수가 작게 보이는 이유는 “저장소 데이터가 거의 없다”는 뜻이 아니라, Git 객체가 pack file 중심으로 묶여 있고 working tree가 생성되지 않았기 때문이다.
즉, 소스 파일 수천 개가 사라진 것이 아니라 체크아웃되지 않은 상태로 <code class="language-plaintext highlighter-rouge">.pack</code> 내부에 압축되어 보관된 것이다.</p>

<p>그래서 bare clone의 비용 절감은 주로 아래 두 축에서 발생한다.</p>

<p>첫째, checkout 대상 파일을 만들지 않으므로 일반 파일 수가 극적으로 줄어든다.
둘째, working tree를 유지하기 위한 추가 파일시스템 I/O가 줄어든다.</p>

<p>이 두 가지는 특히 저장소 파일 수가 많거나, 동일한 작업을 반복 수행하거나, 디스크 I/O가 병목인 환경에서 의미를 가진다.</p>

<h3 id="너무-많은-기대">너무 많은 기대</h3>
<hr />
<p>필자의 경우 “working clone은 파일을 엄청 많이 만들기 때문에 bare clone보다 훨씬 느릴 것”이라고 예상했다.
그러나 이 직관은 절반만 맞다.</p>

<p>실제 clone 시간은 checkout 비용만으로 결정되지 않는다.
clone 전체 시간에는 원격 서버와의 통신, 인증, refs 광고, 객체 협상, pack 생성, pack 다운로드, object database 기록, 그리고 마지막 checkout까지 여러 단계가 함께 포함된다.</p>

<p>이 말은 곧, working clone이 추가적인 파일시스템 비용을 가지는 것은 맞지만, 전체 시간에서 그 비중이 항상 압도적이지는 않다는 뜻이다.
네트워크 상태가 지배적이거나, 서버 측 pack 생성 시간이 큰 경우, 또는 로컬 디스크가 충분히 빠른 경우에는 working clone과 bare clone의 시간 차이가 생각보다 작게 나타날 수 있다.</p>

<p>즉, <code class="language-plaintext highlighter-rouge">파일 수 차이</code> 와 <code class="language-plaintext highlighter-rouge">시간 차이</code> 는 같은 방향으로 움직일 가능성은 높지만, 항상 같은 비율로 벌어지지는 않는다.
실무에서 bare clone의 가치를 설명할 때도 시간만 강조하기보다, 디스크 사용량 감소와 checkout 제거라는 구조적 차이를 함께 설명하는 편이 더 설득력이 있다.</p>

<h3 id="bare-clone-가능한-경우">Bare Clone 가능한 경우</h3>
<hr />
<p>여기서 가장 중요한 결론이 나온다.
bare clone이 더 가벼워 보인다고 해서 모든 clone 경로를 bare로 통일하는 것은 좋은 전략이 아니다.</p>

<p>기준은 단순하다.
그 작업이 <code class="language-plaintext highlighter-rouge">working tree를 실제로 필요로 하는가</code>를 먼저 봐야 한다.</p>

<p>예를 들어 다음과 같은 작업은 bare clone과 잘 맞는다.</p>

<p>ref 조회나 브랜치 상태 비교처럼 Git metadata만 읽는 작업이 그렇다.
또는 fetch 이후 commit graph를 비교하거나, 특정 브랜치 간 변경 파일 목록을 산출하는 작업도 candidate가 될 수 있다.
이 경우 애초에 Java 소스 파일이나 YAML 파일을 실제 경로에서 열지 않기 때문에, working tree를 만드는 행위 자체가 비용만 추가할 수 있다.</p>

<p>반대로 다음과 같은 작업은 bare clone으로 바꾸기 어렵다.</p>

<p>소스 파일을 실제 경로로 읽어야 하는 정적 분석 작업이 그렇다.
빌드 도구를 실행해야 하는 테스트 경로도 마찬가지다.
클론한 디렉터리 안에서 압축을 풀거나 파일을 수정한 뒤 commit/push 하는 흐름 역시 working tree가 반드시 필요하다.</p>

<p>결국 bare clone은 “더 좋은 clone”이라기보다 “특정 작업에 더 적합한 clone”이다.
모든 곳에 적용하는 범용 최적화가 아니라, metadata 중심 작업을 분리해 적용하는 선택적 최적화에 가깝다.</p>

<!-- ### 비용 절감 사례는 어떻게 정리하면 좋은가 -->
<!-- --- -->
<!-- 만약 팀 차원에서 "기존에는 working clone을 사용했지만, bare clone으로 전환하여 비용을 줄였다"는 내용을 정리하고 싶다면, 그 문장은 반드시 범위를 제한해서 써야 한다. -->
<!---->
<!-- 예를 들어 아래와 같은 서술이 더 안전하다. -->
<!---->
<!-- working tree가 필요하지 않은 Git metadata 중심 작업을 식별한 뒤, 해당 경로에 한해 working clone을 bare clone으로 전환하였다. -->
<!-- 그 결과 생성 파일 수와 디스크 사용량을 크게 줄일 수 있었고, clone 소요 시간도 일부 절감할 수 있었다. -->
<!-- 다만 소스 파일 직접 접근, 빌드, 테스트, 파일 수정이 필요한 경로는 기존 working clone을 유지하였다. -->
<!---->
<!-- 이 정리는 두 가지 점에서 중요하다. -->
<!-- 하나는 bare clone의 장점을 과장하지 않는다는 점이다. -->
<!-- 다른 하나는 시스템 기능을 깨지 않으면서도 최적화 포인트를 정확히 좁혔다는 점이다. -->
<!---->
<!-- 특히 비용 절감 항목은 아래 세 축으로 정리하면 좋다. -->
<!---->
<!-- 첫째, 결과 디렉터리 크기가 얼마나 줄었는가. -->
<!-- 둘째, 생성 파일 수가 얼마나 줄었는가. -->
<!-- 셋째, 전체 clone 시간이 어느 정도 줄었는가. -->
<!---->
<!-- 여기서 가장 안정적으로 설명 가능한 지표는 대개 크기와 파일 수다. -->
<!-- 시간은 환경, 네트워크, 캐시의 영향을 크게 받기 때문에 "유의미한 감소가 관측되었다" 정도로 표현하는 편이 보수적이다. -->

<!-- ### 정리하면서 남는 한 가지 질문 -->
<!-- --- -->
<!-- bare clone을 적용할 수 없는 경로가 많다면, 중간 대안은 없을까. -->
<!---->
<!-- 이 질문에 대한 현실적인 답 중 하나는 `no-checkout clone` 이다. -->
<!-- 이 방식은 Git metadata는 먼저 받아두되, working tree를 즉시 만들지 않는 방식으로 접근할 수 있다. -->
<!-- 즉, bare clone만큼 극단적이지 않으면서도 checkout 비용을 뒤로 미룰 수 있다. -->
<!---->
<!-- 그래서 실무에서는 `working clone` 과 `bare clone` 사이에 `no-checkout clone` 같은 완충 지점을 두고, 실제 호출부의 요구사항에 맞게 분기하는 방식이 더 현실적일 수 있다. -->
<!-- 모든 경로를 bare로 바꾸는 것보다, 어떤 경로는 bare, 어떤 경로는 no-checkout, 어떤 경로는 기존 working clone을 유지하는 식의 다층 전략이 오히려 시스템 친화적이다. -->

<h3 id="conclusion">Conclusion</h3>
<hr />
<!-- bare clone은 분명 매력적이다. -->

<!-- 하지만 이 장점은 "모든 clone에 적용 가능한 절대 해법"으로 받아들일 때 오히려 독이 된다. -->
<!-- 실제 시스템에서는 어떤 작업은 working tree를 반드시 필요로 하고, 어떤 작업은 `.git` 정보만으로 충분하다. -->
<!---->
<!-- 그래서 더 정확한 질문은 "`bare clone이 더 좋은가?`"가 아니라 "`이 작업은 working tree가 정말 필요한가?`"에 가깝다. -->
<!-- 이 질문에 답할 수 있을 때 비로소 bare clone은 안전한 최적화가 된다. -->

<p>bare clone은 working tree를 만들지 않기 때문에 생성 파일 수를 극적으로 줄일 수 있고, 디스크 사용량과 checkout 비용도 함께 낮출 수 있다.
하지만 모든 행위에 대해 적용 가능한 절대 해법이 아니라, 어떤 행위를 수행하는지에 따라 clone 방식이 결정되어야 한다.</p>]]></content><author><name>HwiRyung Kim</name></author><category term="git" /><category term="backend" /><summary type="html"><![CDATA[Overview Git 저장소를 다룰 때 우리는 습관적으로 git clone 결과를 작업 디렉터리까지 포함한 형태로 받아온다. 이 방식은 익숙하고 직관적이다. 클론이 끝나면 곧바로 소스 파일을 열어볼 수 있고, 수정도 가능하며, 빌드나 테스트도 바로 수행할 수 있다.]]></summary></entry><entry><title type="html">mesh series: mTLS mechanism with envoy</title><link href="https://hrllk.github.io/mtls-mechanism-with-envoy.html" rel="alternate" type="text/html" title="mesh series: mTLS mechanism with envoy" /><published>2026-03-10T00:00:00+00:00</published><updated>2026-03-10T00:00:00+00:00</updated><id>https://hrllk.github.io/mTLS-mechanism-with-envoy</id><content type="html" xml:base="https://hrllk.github.io/mtls-mechanism-with-envoy.html"><![CDATA[<h3 id="overview">Overview</h3>
<hr />
<p>지난 포스팅에서 Envoy가 80 패킷을 가로채 443으로 업그레이드하는 과정을 살펴봤다. 
그런데 여기서 핵심적인 질문이 남는다.</p>

<blockquote>
  <p>“Envoy가 사용하는 인증서는 누가 주는 걸까? 그리고 수신측 사이드카는 이 인증서를 어떻게 믿는 거지?”</p>
</blockquote>

<p>단순히 패킷을 암호화하는 것을 넘어, 서로가 서로를 신뢰할 수 있게 만드는 <strong>mTLS(Mutual TLS)와 Istio의 신뢰 구축 매커니즘</strong>을 파헤쳐 본다.</p>

<h3 id="신뢰의-기원-istiod와-ca">신뢰의 기원: Istiod와 CA</h3>
<hr />
<p>사이드카가 스스로 인증서를 생성하는 것은 아니다. Istio 시스템의 관제탑인 <strong>Istiod</strong>가 그 역할을 수행한다.</p>

<ol>
  <li><strong>인증서 발급 요청 (CSR):</strong> 사이드카(Envoy)가 처음 실행될 때, <code class="language-plaintext highlighter-rouge">Istiod</code>에게 “나 서비스 A인데, 내가 사용할 인증서 좀 줘!”라고 요청한다.</li>
  <li><strong>신원 확인:</strong> <code class="language-plaintext highlighter-rouge">Istiod</code>는 해당 파드가 정말 서비스 A가 맞는지 쿠버네티스 API를 통해 확인한다.</li>
  <li><strong>인증서 배달:</strong> 확인이 완료되면 <code class="language-plaintext highlighter-rouge">Istiod</code>는 해당 사이드카에게만 유효한 <strong>짧은 수명의 인증서</strong>를 안전하게 전달한다.</li>
</ol>

<p>이때 인증서 안에는 <strong>SPIFFE ID</strong>라는 표준 형식이 담기며, 이것이 서비스 메시 안에서의 ‘신분증’ 역할을 한다.</p>

<h3 id="mtls-서로의-신분증을-확인하는-양방향-검증-zero-trust">mTLS: 서로의 신분증을 확인하는 양방향 검증 (Zero Trust)</h3>
<hr />
<p>일반적인 HTTPS(TLS)는 클라이언트가 서버만 확인하지만, <strong>mTLS(Mutual TLS)</strong>는 ‘양방향’이다.</p>

<ul>
  <li><strong>Client Envoy:</strong> “내 신분증 여기 있어. 너도 신분증 보여줘.”</li>
  <li><strong>Server Envoy:</strong> “나도 신분증 있어. 자, 여기. 너도 내가 믿는 <code class="language-plaintext highlighter-rouge">Istiod</code>가 발급해준 게 맞네!”</li>
</ul>

<p>두 사이드카가 서로의 인증서를 대조하고, 둘 다 동일한 뿌리(Root CA)에서 발급된 것임을 확인하는 순간 <strong>암호화된 터널</strong>이 뚫린다.</p>

<h3 id="왜-mtls가-강력한가">왜 mTLS가 강력한가?</h3>
<hr />
<ol>
  <li><strong>중간자 공격(MITM) 방지:</strong> 트래픽을 가로채도 암호화되어 읽을 수 없다.</li>
  <li><strong>신원 도용 방지:</strong> 인증서가 없는 가짜 서비스는 메시 안에서 통신 자체가 불가능하다.</li>
  <li><strong>자동 갱신:</strong> 개발자가 신경 쓰지 않아도 <code class="language-plaintext highlighter-rouge">Istiod</code>가 주기적으로 인증서를 갈아치우기 때문에 보안 수준이 항상 높게 유지된다.</li>
</ol>

<h3 id="conclusion">Conclusion</h3>
<hr />
<p>결국 서비스 메시에서의 보안은 <strong>“누구도 믿지 않는다(Zero Trust)”</strong>는 원칙에서 시작한다.</p>

<p>모든 서비스는 매번 통신할 때마다 자신의 신원을 증명해야 하며, 이 복잡한 과정을 개발자 대신 사이드카와 Istiod가 처리해 준다. 우리는 그저 안전하게 구축된 신뢰의 도로 위를 달리기만 하면 된다.</p>]]></content><author><name>HwiRyung Kim</name></author><category term="cs" /><category term="network" /><summary type="html"><![CDATA[Overview 지난 포스팅에서 Envoy가 80 패킷을 가로채 443으로 업그레이드하는 과정을 살펴봤다. 그런데 여기서 핵심적인 질문이 남는다.]]></summary></entry><entry><title type="html">mesh series: envoy protocol upgrade(packet boxing unboxing)</title><link href="https://hrllk.github.io/envoy-protocol-boxing-unboxing.html" rel="alternate" type="text/html" title="mesh series: envoy protocol upgrade(packet boxing unboxing)" /><published>2026-03-09T00:00:00+00:00</published><updated>2026-03-09T00:00:00+00:00</updated><id>https://hrllk.github.io/envoy-protocol-boxing-unboxing</id><content type="html" xml:base="https://hrllk.github.io/envoy-protocol-boxing-unboxing.html"><![CDATA[<h3 id="overview">Overview</h3>
<hr />

<p>지난 포스팅에서 <code class="language-plaintext highlighter-rouge">iptables</code>가 어떻게 트래픽을 가로채 Envoy(Sidecar)에게 배달하는지 살펴봤다. 
가로채기(Intercept)가 성공했다면, 이제 Envoy는 이 패킷을 가공할 차례다.</p>

<p>앱 로그에는 분명 <code class="language-plaintext highlighter-rouge">http://...:80</code>으로 찍히는데, 실제 네트워크 장비에서는 왜 <code class="language-plaintext highlighter-rouge">443(TLS)</code> 패킷만 보이는 걸까? 
이번 포스트에서는 Plain HTTP가 mTLS 암호화 통신으로 둔갑하는 <strong>‘프로토콜 업그레이드’</strong> 과정을 다룬다.</p>

<h3 id="패킷의-여정-step-by-step">패킷의 여정: Step-by-Step</h3>
<hr />

<h4 id="1-출항-app-a-inside-pod">1. 출항 (App A inside Pod)</h4>
<p>앱 컨테이너는 평소처럼 <strong>HTTP/1.1 80</strong> 패킷을 생성한다.</p>
<ul>
  <li><strong>Source:</strong> <code class="language-plaintext highlighter-rouge">App A</code></li>
  <li><strong>Protocol:</strong> <code class="language-plaintext highlighter-rouge">HTTP (Plain Text)</code></li>
  <li><strong>Payload:</strong> <code class="language-plaintext highlighter-rouge">"Hello, Service B!"</code></li>
</ul>

<h4 id="2-검문소-진입-sidecar-a">2. 검문소 진입 (Sidecar A)</h4>
<p><code class="language-plaintext highlighter-rouge">iptables</code>에 의해 가로채진 패킷이 Sidecar A(Envoy)에 도착한다. 
Envoy는 이 패킷을 분석하고 다음과 같이 변환한다.</p>
<ul>
  <li><strong>Protocol Upgrade:</strong> HTTP 요청을 <strong>mTLS(Mutual TLS)</strong> 터널 안으로 집어넣는다. (Encapsulation)</li>
  <li><strong>Identity 추가:</strong> 자신이 <code class="language-plaintext highlighter-rouge">Istiod</code>로부터 발급받은 인증서를 패킷에 실어 ‘나 믿을만한 놈이야’라고 증명한다.</li>
</ul>

<h4 id="3-망해network-진출">3. 망해(Network) 진출</h4>
<p>이제 Pod 밖으로 나가는 패킷은 더 이상 80이 아니다.</p>
<ul>
  <li><strong>Protocol:</strong> <strong>HTTPS (TLS v1.3)</strong></li>
  <li><strong>Destination Port:</strong> <strong>15008</strong> (Istio mTLS 기본 포트) 또는 443</li>
  <li>이때 외부에서 <code class="language-plaintext highlighter-rouge">tcpdump</code>로 패킷을 가로채면, 내용은 암호화되어 있어 절대 읽을 수 없다.</li>
</ul>

<h3 id="수신측의-반전-sidecar-b의-역할">수신측의 반전: Sidecar B의 역할</h3>
<hr />

<p>패킷이 목적지인 Service B의 Pod에 도착하면 반대의 마법이 일어난다.</p>

<ol>
  <li><strong>Inbound 가로채기:</strong> Sidecar B가 들어오는 443(mTLS) 패킷을 가로챈다.</li>
  <li><strong>복호화 (Downgrade):</strong> 자신의 인증서로 상대방(Sidecar A)을 검증하고, 암호화된 옷을 벗겨낸다.</li>
  <li><strong>배달:</strong> 원래의 <strong>HTTP/1.1 80</strong> 상태로 복구된 패킷을 내부 앱 컨테이너(<code class="language-plaintext highlighter-rouge">localhost</code>)로 던져준다.</li>
</ol>

<h3 id="왜-프로토콜-업그레이드인가">왜 ‘프로토콜 업그레이드’인가?</h3>
<hr />

<p>이 매커니즘의 핵심은 <strong>“앱은 요청의 시작과 끝에서 오직 80만 본다”</strong>는 점이다.</p>

<ul>
  <li><strong>App A (Client):</strong> “난 80으로 보냈어.”</li>
  <li><strong>App B (Server):</strong> “난 80으로 받았어.”</li>
  <li><strong>네트워크 (Network):</strong> “내 구간에서는 오직 강력한 암호화(443/mTLS)만 흘렀어.”</li>
</ul>

<p>이것이 바로 개발자가 코드를 고치지 않고도 보안 요구사항을 완벽히 충족할 수 있는 이유, 즉 <strong>인프라에 의한 프로토콜 업그레이드</strong>다.</p>

<h3 id="conclusion">Conclusion</h3>
<hr />

<p>결국 서비스 메시 환경에서 통신 보안이란, 양쪽 끝단(App)을 속이지 않으면서 중간 통로(Network)만 안전한 터널로 교체하는 정교한 작업이다. 
우리는 이 과정을 통해 <strong>보안 강화</strong>와 <strong>개발 생산성</strong>이라는 두 마리 토끼를 모두 잡을 수 있었다.</p>

<h3 id="next">Next</h3>
<hr />

<p>다음 포스트에서는 <strong><a href="/mtls-mechanism-with-envoy.html">mTLS 동작 원리와 내부 신뢰 구축 과정</a></strong>을 설명한다.</p>

<p>단순히 암호화만 하는 것이 아니라,<br />
서로가 서로를 어떻게 믿고 통신을 허락하는지 그 ‘신뢰(Trust)’의 기술적 메커니즘을 다룬다.</p>]]></content><author><name>HwiRyung Kim</name></author><category term="cs" /><category term="network" /><summary type="html"><![CDATA[Overview]]></summary></entry><entry><title type="html">mesh series: envoy how to intercept in, out bound traffic?</title><link href="https://hrllk.github.io/envoy-how-to-intercept.html" rel="alternate" type="text/html" title="mesh series: envoy how to intercept in, out bound traffic?" /><published>2026-03-07T00:00:00+00:00</published><updated>2026-03-07T00:00:00+00:00</updated><id>https://hrllk.github.io/envoy-how-to-intercept</id><content type="html" xml:base="https://hrllk.github.io/envoy-how-to-intercept.html"><![CDATA[<h3 id="overview">Overview</h3>
<hr />
<p>지난 포스팅에서 보안의 주도권을 앱이 아닌 인프라(Sidecar)로 옮기기로 결정되었다.
이제 개발자는 평소처럼 80 포트로 호출하면 된다. (어떻게 설정했는지 본문에서는 다루지 않는다.)</p>

<p>그런데 여기서 의문이 생겼다.</p>

<blockquote>
  <p>“내 앱 컨테이너는 그냥 외부로 패킷을 던졌을 뿐인데, 옆에 있는 Sidecar(Envoy)가 이걸 어떻게 가로채는 거지?”</p>
</blockquote>

<p>본문에서는 그 ‘가로채기(Intercept)’의 비밀인 <strong>Transparent Proxy</strong> 매커니즘을 간략히 살펴본다.</p>

<h3 id="같은-집pod-다른-방-sidecar-구조">같은 집(Pod), 다른 방: Sidecar 구조</h3>
<hr />
<p>쿠버네티스에서 <strong>Pod</strong>는 하나의 가상 호스트와 같다. 하나의 Pod 안에 우리 앱 컨테이너와 Envoy 컨테이너가 함께 담기면 다음과 같은 특징을 갖는다.</p>

<ul>
  <li>
    <p><strong>Network Namespace 공유:</strong> 두 컨테이너는 같은 IP와 포트 공간을 공유한다. 즉, 서로 <code class="language-plaintext highlighter-rouge">localhost</code>로 통신이 가능하다.</p>
  </li>
  <li><strong>프로세스 및 파일시스템 격리:</strong> 같은 IP를 사용하지만, 두 컨테이너는 엄격히 분리된 환경이다.
    <ul>
      <li>앱 컨테이너는 Envoy가 사용하는 메모리나 파일을 볼 수 없고, 실행 중인 프로세스 목록(ps)에도 서로가 나타나지 않는다. (프로세스는 격리)</li>
    </ul>
  </li>
  <li>즉, 네트워크만 공유되고 그 외는 격리된 상태이다.</li>
</ul>

<h3 id="transparent-proxy-iptables-주입">Transparent Proxy: iptables 주입</h3>
<hr />
<blockquote>
  <p>앱이 모르게 트래픽을 가로채는 핵심은 리눅스 커널의 네트워크 관리 도구인 <strong>iptables</strong>다.</p>
</blockquote>

<p>사이드카가 주입(Injection)될 때, Istio는 <code class="language-plaintext highlighter-rouge">istio-init</code>이라는 컨테이너를 통해 대상 Pod의 커널에 <code class="language-plaintext highlighter-rouge">iptables</code> 규칙을 함께 주입한다고 한다. 이후 앱의 모든 인바운드/아웃바운드 트래픽은 이 규칙에 따라 인터셉트된다.</p>

<ol>
  <li>
    <p><strong>아웃바운드 가로채기:</strong> 앱이 밖으로 보내는 80 트래픽을 가로채 <strong>사이드카(Envoy)의 15001번 포트</strong>로 강제 포워딩된다. 여기서 Envoy는 목적지를 확인하고 <strong>mTLS 암호화</strong> 후에 포워딩된다.</p>
  </li>
  <li>
    <p><strong>인바운드 가로채기:</strong> 외부에서 들어오는 트래픽을 <strong>사이드카의 15006번 포트</strong>로 먼저 꺾여 여기서 Envoy는 암호를 풀고(복호화) 정상적인 요청인지 확인한 뒤, 내부 앱의 80 포트로 최종 전달한다.</p>
  </li>
</ol>

<p>이것을 <strong>Transparent Proxy(투명 프록시)</strong>라고 부른다. 앱 입장에서는 평소처럼 통신했을 뿐인데, 패킷이 커널 계층에서 Envoy라는 ‘검문소’로 배달되는 매커니즘이다.</p>

<h3 id="서비스-간-통신-흐름-service-a-to-service-b">서비스 간 통신 흐름 (Service A to Service B)</h3>
<hr />
<p>앱 A가 앱 B를 호출할 때, 각 사이드카가 어떻게 협력하는지 시퀀스로 나타내면 다음과 같다.</p>

<ol>
  <li><strong>App A</strong>: <code class="language-plaintext highlighter-rouge">http://service-b:80</code>으로 요청 전송</li>
  <li><strong>Sidecar A (Intercept)</strong>: Outbound 규칙에 의해 요청을 가로챔</li>
  <li><strong>Sidecar A (Upgrade)</strong>: HTTP(80) 요청을 mTLS(443)로 감싸서 전송</li>
  <li><strong>Sidecar B (Intercept)</strong>: Inbound 규칙에 의해 들어오는 443 요청을 가로챔</li>
  <li><strong>Sidecar B (Downgrade)</strong>: mTLS(443)를 복호화하여 원래의 HTTP(80)로 변환</li>
  <li><strong>App B</strong>: Sidecar B로부터 순수한 HTTP(80) 요청을 수신</li>
</ol>

<h3 id="envoy-role-intercept-이후">Envoy Role (Intercept 이후)</h3>
<hr />
<p>일단 트래픽을 가로챈 Envoy는 다음 작업들을 수행한다.</p>

<ul>
  <li><strong>인증서 관리:</strong> <code class="language-plaintext highlighter-rouge">Istiod</code>가 발급해준 인증서를 들고 상대방 Sidecar와 mTLS 터널을 맺는다.</li>
  <li><strong>업그레이드:</strong> 앱이 보낸 Plain HTTP 트래픽을 mTLS(HTTPS)로 감싸서(Wrapping) 내보낸다.</li>
  <li><strong>라우팅:</strong> 목적지가 살아있는지, 어디로 보내야 가장 빠를지 결정</li>
</ul>

<h3 id="conclusion">Conclusion</h3>
<hr />
<p>우리가 소스 코드를 단 한 줄도 고칠 필요가 없었던 이유는, 이 모든 과정이 애플리케이션 계층이 아닌 <strong>리눅스 커널과 네트워크 인터페이스 계층</strong>에서 일어나기 때문이다.</p>

<p>결국 보안 주도권의 전도는 이 iptables라는 보이지 않는 조력자 덕분에 완성된다.</p>

<h3 id="next">Next</h3>
<hr />
<p>다음 포스트에서는 <strong><a href="/envoy-protocol-boxing-unboxing.html">envoy가 패킷을 어떻게 가공하는지</a></strong>를 설명한다.</p>]]></content><author><name>HwiRyung Kim</name></author><category term="cs" /><category term="network" /><summary type="html"><![CDATA[Overview 지난 포스팅에서 보안의 주도권을 앱이 아닌 인프라(Sidecar)로 옮기기로 결정되었다. 이제 개발자는 평소처럼 80 포트로 호출하면 된다. (어떻게 설정했는지 본문에서는 다루지 않는다.)]]></summary></entry><entry><title type="html">mesh series: why do we use a service mesh?</title><link href="https://hrllk.github.io/why-should-use-mesh.html" rel="alternate" type="text/html" title="mesh series: why do we use a service mesh?" /><published>2026-03-06T00:00:00+00:00</published><updated>2026-03-06T00:00:00+00:00</updated><id>https://hrllk.github.io/why-should-use-mesh</id><content type="html" xml:base="https://hrllk.github.io/why-should-use-mesh.html"><![CDATA[<h3 id="overview">Overview</h3>
<hr />
<p>이전 글에서 이번 시리즈의 출발점을 정리했다.<br />
요구사항은 명확하다.</p>

<p><strong>“외부 인입뿐 아니라, 클러스터 내부 서비스 간 통신도 443 기반으로 보호해야 한다.”</strong></p>

<p>가장 먼저 떠오르는 방법은 각 애플리케이션이 직접 HTTPS 서버가 되는 방식이다.<br />
하지만 마이크로서비스 환경에서는 이 접근이 빠르게 한계에 부딪힌다. 이번 글에서는 왜 내부 통신 보안을 애플리케이션이 아니라 <strong>Service Mesh</strong>가 담당해야 하는지 정리한다.</p>

<h3 id="가장-단순한-접근-애플리케이션이-직접-tls를-처리한다">가장 단순한 접근: 애플리케이션이 직접 TLS를 처리한다</h3>
<hr />
<p>직접 구현 방식은 아래와 같다.</p>

<ul>
  <li>각 서비스가 HTTPS 서버로 동작</li>
  <li>서비스별 인증서 발급 및 배포</li>
  <li>클라이언트도 HTTPS로 호출</li>
  <li>통신 시 인증서 검증 로직 수행</li>
</ul>

<p>겉으로는 단순하지만, 서비스 수가 늘어나는 순간 운영 복잡도가 급격히 커진다.</p>

<!-- (실제로 요구사항이 떨어졌을때 모든 애플리케이션간에 인증서설정을 진행하고 테스트했었다.  -->

<h3 id="왜-애플리케이션이-직접-처리하면-어려운가">왜 애플리케이션이 직접 처리하면 어려운가</h3>
<hr />
<p>첫 번째 문제는 <strong>인증서 관리 비용</strong>이다.</p>

<ul>
  <li>서비스마다 인증서 필요</li>
  <li>만료 주기 관리 필요</li>
  <li>배포 시점마다 키와 인증서 동기화 필요</li>
  <li>키 유출 리스크 증가</li>
</ul>

<p>서비스가 많아질수록 발급, 교체, 배포, 만료 대응이 모두 운영 부담으로 바뀐다.</p>

<p>두 번째 문제는 <strong>개발자 책임의 확장</strong>이다.</p>

<p>TLS는 단순히 스위치를 켜는 일이 아니다.</p>
<ul>
  <li>인증서 검증 로직</li>
  <li>Hostname Validation</li>
  <li>Cipher Suite 설정</li>
  <li>TLS 버전 관리</li>
</ul>

<p>이 책임을 각 애플리케이션이 직접 가지면, 개발자는 비즈니스 로직 외에 통신 보안 설정까지 떠안게 된다.</p>

<p>세 번째 문제는 <strong>기술 스택별 편차</strong>다.</p>
<ul>
  <li>Java 서비스와 Node.js 서비스의 TLS 설정 방식은 다르다.</li>
  <li>프레임워크마다 기본값과 보안 옵션도 다르다.</li>
  <li>결국 조직 전체 보안 수준은 가장 약한 서비스에 맞춰질 가능성이 높다.</li>
</ul>

<p>네 번째 문제는 <strong>가시성 부족</strong>이다.<br />
애플리케이션 내부에서 TLS를 처리하면 다음을 바로 확인하기 어렵다.</p>

<ul>
  <li>실제로 암호화가 되고 있는지 확인하기 어려움</li>
  <li>어느 구간이 TLS로 보호되는지</li>
  <li>장애가 발생한 지점이 애플리케이션인지 네트워크인지</li>
  <li>정책이 전체 서비스에 일관되게 적용되었는지</li>
</ul>

<p>즉, 보안은 들어갔지만 <strong>운영과 검증이 어려운 구조</strong>가 된다.</p>

<h3 id="관점의-전환-보안을-네트워크-계층으로-내리면">관점의 전환: 보안을 네트워크 계층으로 내리면?</h3>
<hr />
<p>이 지점에서 질문이 바뀐다.</p>

<p><strong>“TLS를 애플리케이션이 아니라 네트워크 계층이 대신 처리하면 안 될까?”</strong></p>

<p>이 발상에서 출발하는 것이 Service Mesh다. Istio 환경에서는 각 Pod 옆의 Sidecar Proxy(Envoy)가 애플리케이션을 대신해 통신 보안을 담당한다.</p>

<p>즉, 흐름은 아래처럼 바뀐다.</p>

<p><code class="language-plaintext highlighter-rouge">App A → Sidecar A → mTLS → Sidecar B → App B</code></p>

<p>애플리케이션은 기존처럼 HTTP 요청을 보내고, 실제 네트워크 구간은 Sidecar가 mTLS로 처리한다.</p>

<h3 id="왜-service-mesh가-더-적합한가">왜 Service Mesh가 더 적합한가</h3>
<hr />
<p>Service Mesh가 적합한 이유는 명확하다.</p>

<p>첫 번째는 <strong>인증서 관리 자동화</strong>다.</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">Istiod</code>가 인증 기관(CA) 역할을 수행</li>
  <li>Sidecar에 인증서를 자동 발급</li>
  <li>짧은 주기의 인증서를 자동 갱신</li>
</ul>

<p>개발자가 인증서를 직접 다루지 않아도 된다.</p>

<p>두 번째는 <strong>코드 변경 최소화</strong>다.</p>

<ul>
  <li>기존 애플리케이션 호출 구조 유지</li>
  <li>서비스 코드 수정 없이 보안 정책 반영 가능</li>
</ul>

<p>보안 요구사항이 들어와도 비즈니스 코드에 연쇄 수정이 퍼지지 않는다.</p>

<p>세 번째는 <strong>정책의 일관성</strong>이다.</p>

<ul>
  <li>언어와 프레임워크가 달라도 동일한 보안 정책 적용 가능</li>
  <li>서비스별 편차가 아니라 플랫폼 수준에서 통제 가능</li>
</ul>

<p>조직 전체 보안 수준을 일정하게 유지할 수 있다.</p>

<p>네 번째는 <strong>관측 가능성</strong>이다.</p>

<ul>
  <li>어느 트래픽이 암호화되었는지 확인 가능</li>
  <li>메트릭, 로그, 트레이싱 연계 가능</li>
  <li>장애 원인 추적이 쉬워짐</li>
</ul>

<p>즉, 보안이 단순한 설정값이 아니라 관찰 가능한 동작으로 바뀐다.</p>

<h3 id="핵심-비교">핵심 비교</h3>
<hr />
<ul>
  <li><strong>TLS 적용 위치:</strong> 애플리케이션 직접 TLS는 앱이 처리하고, Service Mesh는 Sidecar Proxy가 처리한다.</li>
  <li><strong>인증서 관리:</strong> 애플리케이션 직접 TLS는 수동 관리가 필요하고, Service Mesh는 자동화할 수 있다.</li>
  <li><strong>코드 수정:</strong> 애플리케이션 직접 TLS는 코드 변경이 필요하지만, Service Mesh는 거의 필요 없다.</li>
  <li><strong>보안 정책 일관성:</strong> 애플리케이션 직접 TLS는 서비스별 편차가 생기기 쉽고, Service Mesh는 일관되게 적용할 수 있다.</li>
  <li><strong>운영 난이도:</strong> 애플리케이션 직접 TLS는 높고, Service Mesh는 상대적으로 낮다.</li>
  <li><strong>관측 가능성:</strong> 애플리케이션 직접 TLS는 낮고, Service Mesh는 높다.</li>
</ul>

<h3 id="conclusion">Conclusion</h3>
<hr />
<p>이번 요구사항의 핵심은 단순히 “HTTPS를 적용하자”가 아니다.<br />
정확히는 <strong>“내부 통신 보안을 개발자 코드가 아니라 플랫폼 레이어에서 일관되게 처리하자”</strong>에 가깝다.</p>

<ul>
  <li>개발자는 기존 애플리케이션 흐름에 집중한다.</li>
  <li>Sidecar는 통신 구간의 보안을 책임진다.</li>
  <li>운영자는 중앙에서 정책을 통제하고 검증할 수 있다.</li>
</ul>

<p>그래서 내부 443 요구사항 앞에서 중요한 선택은 “TLS를 넣을까 말까”가 아니라, <strong>“누가 그 책임을 질 것인가”</strong>다. 그 답이 바로 Mesh다.</p>

<h3 id="next-step">Next Step</h3>
<hr />
<p>다음 포스트에서는 <strong><a href="/envoy-how-to-intercept.html">Envoy는 어떻게 내 트래픽을 가로채는가: Sidecar와 Transparent Proxy의 실제 구조</a></strong>를 다룬다.
왜 Service Mesh가 더 적합한지 이해했다면, 이제 다음 질문은 자연스럽다.<br />
애플리케이션은 평소처럼 요청했을 뿐인데, 실제 네트워크에서는 Envoy가 어떻게 그 흐름을 대신 통제할 수 있는지 구체적으로 살펴본다.</p>]]></content><author><name>HwiRyung Kim</name></author><category term="cs" /><category term="network" /><summary type="html"><![CDATA[Overview 이전 글에서 이번 시리즈의 출발점을 정리했다. 요구사항은 명확하다.]]></summary></entry><entry><title type="html">mesh series: client wants zero trust series(istio service mesh)</title><link href="https://hrllk.github.io/istio-mesh-internal-443-series-overview.html" rel="alternate" type="text/html" title="mesh series: client wants zero trust series(istio service mesh)" /><published>2026-03-05T00:00:00+00:00</published><updated>2026-03-05T00:00:00+00:00</updated><id>https://hrllk.github.io/istio-mesh-internal-443-series-overview</id><content type="html" xml:base="https://hrllk.github.io/istio-mesh-internal-443-series-overview.html"><![CDATA[<h3 id="overview">Overview</h3>
<hr />
<p>최근 인프라 보안 강화의 일환으로 “내부 애플리케이션 간 통신 시에도 80(HTTP)이 아닌 443(HTTPS) 통신을 수행해주세요” 라는 요구사항이 발생했다.</p>

<p>이에 따라 엔지니어팀에서 테스트 환경의 HTTPS 구성과 툴체인 설정을 진행했다. 
이제 중요한 것은 외부로부터의 인바운드 암호화를 넘어, <strong>클러스터 내부망(Pod-to-Pod) 통신을 어떻게 안전하게 보호했는지 개발자 관점에서 상세히 파악해야한다.</strong>
이번 연재를 통해 Istio Service Mesh가 이 요구사항을 어떻게 스마트하게 해결했는지 그 과정을 기록한다.</p>

<h3 id="요구사항-requirements">요구사항 (Requirements)</h3>
<hr />
<p>고객사에서 도출된 핵심 미션과 기술적 목표</p>

<ul>
  <li><strong>내부 통신 보안:</strong> <code class="language-plaintext highlighter-rouge">xxx-app ↔ xxx-oss</code> 등 내부 서비스 간 호출 시 443 포트 기반의 암호화(mTLS) 적용.</li>
  <li><strong>개발자 관점의 이해:</strong> 인프라에서 설정된 mTLS가 실제 앱 통신에 어떤 영향을 주는지, 소스 코드 수정 없이 어떻게 443 통신이 완성되는지 분석.</li>
  <li><strong>검증 체계 구축:</strong> 엔지니어팀이 구성한 툴체인을 활용하여 실제 암호화 여부를 확인하고 장애 지점을 파악.</li>
</ul>

<h3 id="연재-계획">연재 계획</h3>
<hr />

<ol>
  <li><strong>[프롤로그] 왜 이 시리즈를 시작했는가: 내부 443 통신 요구사항과 Service Mesh라는 선택</strong><br />
<a href="/istio-mesh-internal-443-series-overview.html"><em>현재 글 보기</em></a></li>
  <li><strong>[1편] 왜 앱이 아니라 Mesh가 책임져야 하는가: 내부 HTTPS 요구사항의 해법</strong><br />
<a href="/why-should-use-mesh.html">/why-should-use-mesh.html</a></li>
  <li><strong>[2편] Envoy는 어떻게 내 트래픽을 가로채는가: Sidecar와 Transparent Proxy의 실제 구조</strong><br />
<a href="/envoy-how-to-intercept.html">/envoy-how-to-intercept.html</a></li>
  <li><strong>[3편] 앱은 80으로 보냈는데 왜 네트워크는 443으로 흐를까: Envoy의 프로토콜 업그레이드</strong><br />
<a href="/envoy-protocol-boxing-unboxing.html">/envoy-protocol-boxing-unboxing.html</a></li>
  <li><strong>[4편] Envoy는 상대를 어떻게 믿는가: mTLS와 Istiod 기반 신뢰 체계의 내부 동작</strong><br />
<a href="/mtls-mechanism-with-envoy.html">/mtls-mechanism-with-envoy.html</a></li>
</ol>

<h3 id="약속-our-principle">약속 (Our Principle)</h3>
<hr />

<ul>
  <li><strong>추상화:</strong> 보안은 인프라 영역으로 밀어내고, 앱 개발자는 비즈니스 로직에만 집중을 목표로한다.</li>
  <li><strong>명확성:</strong> 설정으로만 존재하는 보안이 아니라, 관측 도구로 증명 가능한 보안을 지향한다.</li>
  <li><strong>학습:</strong> 엔지니어팀의 결과물을 단순히 수용하지 않고, 동작 원리를 상세히 이해하여 운영 역량을 내재화를 목표한다.</li>
</ul>

<h3 id="next-step">Next Step</h3>
<hr />
<p>다음 포스트에서는<a href="/why-should-use-mesh.html">왜 앱이 아니라 Mesh가 책임져야 하는가: 내부 HTTPS 요구사항의 해법</a>를 다룬다.
기존 방식처럼 모든 마이크로서비스에 직접 SSL/TLS 설정을 관리할 때 발생하는 운영 리스크와, 이를 해결하는 Mesh의 핵심 가치를 비교한다.</p>]]></content><author><name>HwiRyung Kim</name></author><category term="cs" /><category term="network" /><summary type="html"><![CDATA[Overview 최근 인프라 보안 강화의 일환으로 “내부 애플리케이션 간 통신 시에도 80(HTTP)이 아닌 443(HTTPS) 통신을 수행해주세요” 라는 요구사항이 발생했다.]]></summary></entry></feed>