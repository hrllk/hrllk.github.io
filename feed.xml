<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="3.10.0">Jekyll</generator><link href="https://hrllk.github.io/feed.xml" rel="self" type="application/atom+xml" /><link href="https://hrllk.github.io/" rel="alternate" type="text/html" /><updated>2026-09-03T10:15:25+00:00</updated><id>https://hrllk.github.io/feed.xml</id><title type="html">knowledge.log</title><subtitle>knowledge.log
</subtitle><author><name>HwiRyung Kim</name></author><entry><title type="html">모던 GitHub Actions CI Flow: Workflow Quick Started</title><link href="https://hrllk.github.io/github-actions-workflow-writing.html" rel="alternate" type="text/html" title="모던 GitHub Actions CI Flow: Workflow Quick Started" /><published>2026-08-21T00:00:00+00:00</published><updated>2026-08-21T00:00:00+00:00</updated><id>https://hrllk.github.io/github-actions-workflow-writing</id><content type="html" xml:base="https://hrllk.github.io/github-actions-workflow-writing.html"><![CDATA[<h3 id="toc">TOC</h3>
<hr />

<ol>
  <li><a href="#overview">Overview</a></li>
  <li><a href="#workflow-기본-구조">Workflow 기본 구조</a></li>
  <li><a href="#event별-workflow-작성">Event별 Workflow 작성</a>
    <ol>
      <li><a href="#pull_request">pull_request</a></li>
      <li><a href="#push">push</a></li>
      <li><a href="#workflow_dispatch">workflow_dispatch</a></li>
    </ol>
  </li>
  <li><a href="#step-작성">Step 작성</a>
    <ol>
      <li><a href="#run과-uses">run과 uses</a></li>
    </ol>
  </li>
  <li><a href="#최소-workflow-이벤트별-예시">최소 Workflow 이벤트별 예시</a></li>
  <li><a href="#conclusion">Conclusion</a></li>
</ol>

<h3 id="overview">Overview</h3>
<hr />

<p>이 글은 GitHub Actions CI 시리즈의 2편이다. 1편 <a href="/github-standard-ci-flow.html">모던 GitHub Actions CI Flow</a>에서 설명한 흐름을 Workflow YAML로 작성한다. Jenkins의 Pipeline이 실행할 작업을 정의하듯, GitHub Actions Workflow는 Event, Job, Step을 저장소 안에 선언한다.</p>

<p>이 글에서는 <code class="language-plaintext highlighter-rouge">pull_request</code>, <code class="language-plaintext highlighter-rouge">push</code>, <code class="language-plaintext highlighter-rouge">workflow_dispatch</code>를 기준으로 실행 조건을 작성하고, Step에서 <code class="language-plaintext highlighter-rouge">run</code>과 <code class="language-plaintext highlighter-rouge">uses</code>를 사용하는 방법을 설명한다.</p>

<h3 id="workflow-기본-구조">Workflow 기본 구조</h3>
<hr />

<p>Workflow 파일은 저장소의 <code class="language-plaintext highlighter-rouge">.github/workflows</code> 디렉터리에 둔다. 기본 구조는 <code class="language-plaintext highlighter-rouge">name</code>, <code class="language-plaintext highlighter-rouge">on</code>, <code class="language-plaintext highlighter-rouge">jobs</code>로 구성한다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">name</span><span class="pi">:</span> <span class="s">CI</span>

<span class="c1">######</span>
<span class="c1">## 이벤트 선언</span>
<span class="c1">######</span>
<span class="na">on</span><span class="pi">:</span>
  <span class="na">pull_request</span><span class="pi">:</span>
  <span class="c1"># or</span>
  <span class="na">push</span><span class="pi">:</span>
  <span class="c1"># or</span>
  <span class="na">workflow_dispatch</span><span class="pi">:</span>

<span class="c1">######</span>
<span class="c1">## 작업 선언</span>
<span class="c1">######</span>
<span class="na">jobs</span><span class="pi">:</span>
  <span class="na">build-and-test</span><span class="pi">:</span>
    <span class="na">runs-on</span><span class="pi">:</span> <span class="s">ubuntu-latest</span>
    <span class="na">steps</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="na">run</span><span class="pi">:</span> <span class="s">npm test</span>
</code></pre></div></div>

<ul>
  <li><code class="language-plaintext highlighter-rouge">name</code>: GitHub Actions 화면에 표시할 Workflow 이름</li>
  <li><code class="language-plaintext highlighter-rouge">on</code>: Workflow를 실행할 Event 필터</li>
  <li><code class="language-plaintext highlighter-rouge">jobs</code>: Runner에서 실행할 Job과 실행 순서</li>
  <li><code class="language-plaintext highlighter-rouge">steps</code>: Job 안에서 순서대로 실행할 작업</li>
</ul>

<h3 id="event별-workflow-작성">Event별 Workflow 작성</h3>
<hr />

<p>Event는 Jenkins의 Trigger와 비슷한 역할을 한다. 하나의 Workflow에 여러 Event를 선언하면 선언된 Event 중 하나가 발생할 때 Workflow가 실행된다. <a href="https://docs.github.com/en/actions/reference/workflows-and-actions/events-that-trigger-workflows">GitHub Actions Event 문서</a></p>

<h4 id="pull_request">pull_request</h4>

<p>PR 생성, 새 commit Push, 재오픈을 대상으로 CI를 실행</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">on</span><span class="pi">:</span>
  <span class="na">pull_request</span><span class="pi">:</span>
    <span class="na">types</span><span class="pi">:</span> <span class="pi">[</span><span class="nv">opened</span><span class="pi">,</span> <span class="nv">synchronize</span><span class="pi">,</span> <span class="nv">reopened</span><span class="pi">]</span>
</code></pre></div></div>

<ul>
  <li><code class="language-plaintext highlighter-rouge">opened</code>: PR 생성될 때</li>
  <li><code class="language-plaintext highlighter-rouge">synchronize</code>: PR source branch에 새 커밋이 반영될때</li>
  <li><code class="language-plaintext highlighter-rouge">reopened</code>: 닫힌 PR 재오픈될 때</li>
</ul>

<h4 id="push">push</h4>

<p>지정한 브랜치에 commit이 반영될 때 실행 
PR Merge 후 <code class="language-plaintext highlighter-rouge">develop</code>의 상태를 검증 or 개발 환경에 배포할 때 사용할 수 있다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">on</span><span class="pi">:</span>
  <span class="na">push</span><span class="pi">:</span>
    <span class="na">branches</span><span class="pi">:</span> <span class="pi">[</span><span class="nv">develop</span><span class="pi">]</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">branches</code>를 생략하면 모든 브랜치의 Push가 대상이 됨.
운영 배포처럼 영향 범위가 큰 작업은 대상 브랜치를 명시</p>

<h4 id="workflow_dispatch">workflow_dispatch</h4>

<p>GitHub UI나 API에서 Workflow를 수동 실행 
특정 Ref나 입력값을 받아 배포와 운영 작업을 실행할 수 있다.
이때inputs 하위의 custom 인자를 함께 받아 사용할 수 있다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">on</span><span class="pi">:</span>
  <span class="na">workflow_dispatch</span><span class="pi">:</span>
    <span class="na">inputs</span><span class="pi">:</span>
</code></pre></div></div>

<h3 id="step-작성">Step 작성</h3>
<hr />

<p>Step은 Runner에서 직접 명령을 구성하거나,  이미 정의된 자동화 단위를 호출</p>

<h4 id="run과-uses">run과 uses</h4>

<p><code class="language-plaintext highlighter-rouge">run</code>은 Runner의 셸에서 명령을 실행하고, <code class="language-plaintext highlighter-rouge">uses</code>는 Action 또는 재사용 Workflow를 호출</p>

<p><code class="language-plaintext highlighter-rouge">uses</code>는 미리 정의된 동작을 가져와 사용하는 방식이다. 직접 실행 명령을 작성하는 대신, 반복해서 사용할 수 있는 Action을 조합한다는 점에서 동작 템플릿처럼 이해할 수 있다.</p>

<p>Step 수준의 <code class="language-plaintext highlighter-rouge">uses</code>는 Action을 호출한다. 재사용 Workflow는 Job 수준에서 별도의 <code class="language-plaintext highlighter-rouge">uses</code> 문법으로 호출한다.</p>

<h4 id="예시-uses">예시: uses</h4>
<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">...</span> 

<span class="na">steps</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="na">uses</span><span class="pi">:</span> <span class="s">actions/setup-node@v7</span>
    <span class="na">with</span><span class="pi">:</span>
      <span class="na">node-version</span><span class="pi">:</span> <span class="m">24</span>
</code></pre></div></div>

<ul>
  <li><code class="language-plaintext highlighter-rouge">actions/setup-node</code>: 지정한 Node.js 실행 환경을 준비한다.</li>
  <li><code class="language-plaintext highlighter-rouge">with</code>: Action에 입력값을 전달한다.</li>
  <li><code class="language-plaintext highlighter-rouge">@v6</code>, <code class="language-plaintext highlighter-rouge">@v7</code>: Action의 Ref다. Tag, Branch, Commit SHA를 사용할 수 있다.</li>
</ul>

<h4 id="예시-run">예시: run</h4>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">steps</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="na">run</span><span class="pi">:</span> <span class="s">npm test</span>
</code></pre></div></div>

<h3 id="최소-workflow-이벤트별-예시">최소 Workflow 이벤트별 예시</h3>
<hr />

<h4 id="pr">pr</h4>
<div class="language-yml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">name</span><span class="pi">:</span> <span class="s">pr test</span>

<span class="na">on</span><span class="pi">:</span>
  <span class="na">pull_request</span><span class="pi">:</span>
    <span class="na">types</span><span class="pi">:</span> <span class="pi">[</span><span class="nv">opened</span><span class="pi">,</span> <span class="nv">synchronize</span><span class="pi">,</span> <span class="nv">reopened</span><span class="pi">]</span>

<span class="na">jobs</span><span class="pi">:</span>
  <span class="na">pr-test</span><span class="pi">:</span> 
    <span class="na">runs-on</span><span class="pi">:</span> <span class="s">ubuntu-latest</span>
    <span class="na">steps</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="na">uses</span><span class="pi">:</span> <span class="s">actions/checkout@v6</span>

</code></pre></div></div>

<h4 id="push-1">push</h4>
<div class="language-yml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">name</span><span class="pi">:</span> <span class="s">push test</span>

<span class="na">on</span><span class="pi">:</span>
  <span class="na">push</span><span class="pi">:</span>
    <span class="na">branches</span><span class="pi">:</span> <span class="pi">[</span><span class="nv">develop</span><span class="pi">]</span>

<span class="na">jobs</span><span class="pi">:</span>
  <span class="na">push-test</span><span class="pi">:</span> 
    <span class="na">runs-on</span><span class="pi">:</span> <span class="s">ubuntu-latest</span>
    <span class="na">steps</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="na">uses</span><span class="pi">:</span> <span class="s">actions/checkout@v6</span>

</code></pre></div></div>

<h4 id="workflow_dispatch-1">workflow_dispatch</h4>
<div class="language-yml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">name</span><span class="pi">:</span> <span class="s">manual test</span>

<span class="na">on</span><span class="pi">:</span>
  <span class="na">workflow_dispatch</span><span class="pi">:</span>
    <span class="na">inputs</span><span class="pi">:</span>
      <span class="na">msg</span><span class="pi">:</span>
        <span class="na">description</span><span class="pi">:</span> <span class="s">메세지</span>
        <span class="na">required</span><span class="pi">:</span> <span class="no">false</span>
        <span class="na">type</span><span class="pi">:</span> <span class="s">string</span>

<span class="na">jobs</span><span class="pi">:</span>
  <span class="na">manual-test</span><span class="pi">:</span> 
    <span class="na">runs-on</span><span class="pi">:</span> <span class="s">ubuntu-latest</span>
    <span class="na">steps</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="na">run</span><span class="pi">:</span> <span class="s">echo $</span>

</code></pre></div></div>

<h3 id="conclusion">Conclusion</h3>
<hr />

<p>GitHub Actions Workflow는 Event로 실행 시점을 정의하고, Job과 Step으로 실행할 작업을 구성한다.</p>]]></content><author><name>HwiRyung Kim</name></author><category term="infrastructure" /><summary type="html"><![CDATA[TOC]]></summary></entry><entry><title type="html">모던 GitHub Actions CI Flow: 프롤로그 CI 개요</title><link href="https://hrllk.github.io/github-standard-ci-flow.html" rel="alternate" type="text/html" title="모던 GitHub Actions CI Flow: 프롤로그 CI 개요" /><published>2026-08-21T00:00:00+00:00</published><updated>2026-08-21T00:00:00+00:00</updated><id>https://hrllk.github.io/github-standard-ci-flow</id><content type="html" xml:base="https://hrllk.github.io/github-standard-ci-flow.html"><![CDATA[<h3 id="toc">TOC</h3>
<hr />

<ol>
  <li><a href="#overview">Overview</a></li>
  <li><a href="#시리즈-구성">시리즈 구성</a></li>
  <li><a href="#사전지식">사전지식</a>
    <ol>
      <li><a href="#github-actions-용어">GitHub Actions 용어</a></li>
      <li><a href="#jenkins-개념과-비교">Jenkins 개념과 비교</a></li>
    </ol>
  </li>
  <li><a href="#ci-flow">CI Flow</a>
    <ol>
      <li><a href="#pr-flow">PR Flow</a>
        <ol>
          <li><a href="#특징">특징</a></li>
          <li><a href="#draft-pr-의미">Draft PR 의미</a></li>
          <li><a href="#pr-상태전이">PR 상태전이</a></li>
        </ol>
      </li>
      <li><a href="#push-flow">Push Flow</a>
        <ol>
          <li><a href="#특징-1">특징</a></li>
        </ol>
      </li>
      <li><a href="#workflow-dispatch-flow">Workflow Dispatch Flow</a>
        <ol>
          <li><a href="#특징-2">특징</a></li>
        </ol>
      </li>
    </ol>
  </li>
  <li><a href="#conclusion">Conclusion</a></li>
</ol>

<h3 id="overview">Overview</h3>
<hr />

<p>이 글에서는 GitHub Actions를 통해 CI를 진행할 때의 모던한 CI Flow를 설명한다.</p>

<p>Jenkins에서는 보통 Job 또는 Pipeline을 만들고, SCM 변경을 감지한 뒤 Agent에서 빌드와 테스트를 실행한다. 
GitHub Actions에서는 이 흐름을 저장소 안의 Workflow YAML로 정의하고, 정의된 Workflow는 Event에 의해 Runner 내부에서 실행된다. 
본문에서는 Github Actions의 용어와 개념을 설명한다.</p>

<ul>
  <li><a href="https://docs.github.com/en/actions/get-started/understand-github-actions">GitHub Actions의 핵심 개념</a></li>
</ul>

<h3 id="시리즈-구성">시리즈 구성</h3>
<hr />

<blockquote>
  <p>본문은 전체 CI Flow를 설명하는 1편이다. 여기서 정리한 흐름을 실제 Workflow YAML로 옮기는 방법은 2편에서 설명한다.</p>
</blockquote>

<ul>
  <li>1편 (본문) 모던 GitHub Actions CI Flow
    <ul>
      <li>Event별로 CI를 어떤 식으로 사용하는지 이해한다.</li>
    </ul>
  </li>
  <li>2편 <a href="/github-actions-workflow-writing.html">GitHub Actions Workflow 작성하기</a>
    <ul>
      <li>그 흐름을 Workflow YAML로 어떻게 작성하는지 이해한다.</li>
    </ul>
  </li>
</ul>

<h3 id="사전지식">사전지식</h3>
<hr />

<h4 id="github-actions-용어">GitHub Actions 용어</h4>

<table>
  <thead>
    <tr>
      <th>용어</th>
      <th>의미</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Workflow</td>
      <td>하나의 자동화 흐름을 정의한 YAML 파일</td>
    </tr>
    <tr>
      <td>Event</td>
      <td>Workflow를 실행시키는 트리거 (저장소 활동 or 수동·예약 요청)</td>
    </tr>
    <tr>
      <td>Job</td>
      <td>같은 Runner에서 실행되는 Step들의 묶음</td>
    </tr>
    <tr>
      <td>Runner</td>
      <td>Job을 실행하는 실행 환경</td>
    </tr>
    <tr>
      <td>Step</td>
      <td>셸 명령을 실행하거나 Action을 호출하는 작업 단위</td>
    </tr>
    <tr>
      <td>Action</td>
      <td>Workflow에서 재사용할 수 있는 자동화 단위</td>
    </tr>
  </tbody>
</table>

<p>Workflow는 저장소의 <code class="language-plaintext highlighter-rouge">.github/workflows</code> 디렉터리에 위치한다. 하나의 Workflow에는 하나 이상의 Job이 있고, Job은 여러 Step을 순서대로 실행한다. <a href="https://docs.github.com/en/actions/concepts/workflows-and-actions/workflows">GitHub Workflows 문서</a></p>

<h4 id="jenkins-개념과-비교">Jenkins 개념과 비교</h4>

<table>
  <thead>
    <tr>
      <th>Jenkins</th>
      <th>GitHub Actions</th>
      <th>차이점</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Job 또는 Pipeline</td>
      <td>Workflow</td>
      <td>GitHub Actions Workflow는 저장소에 YAML 관리</td>
    </tr>
    <tr>
      <td>Trigger 또는 SCM Polling/Webhook</td>
      <td>Event</td>
      <td><code class="language-plaintext highlighter-rouge">on</code> 키에 실행 조건 선언</td>
    </tr>
    <tr>
      <td>Agent</td>
      <td>Runner</td>
      <td>GitHub 호스팅 Runner 또는 자체 호스팅 Runner를 선택</td>
    </tr>
    <tr>
      <td>Stage</td>
      <td>Job 또는 Step</td>
      <td>병렬 실행과 의존 관계를 Job 단위로 표현할 수 있다.</td>
    </tr>
  </tbody>
</table>

<p>이 표는 이해를 위한 비교이며, 두 시스템의 설정 단위가 완전히 같은 것은 아니다.</p>

<h3 id="ci-flow">CI Flow</h3>
<hr />
<p>이벤트별 CI 방식을 설명한다.</p>

<h4 id="pr-flow">PR Flow</h4>

<p>예를 들어 <code class="language-plaintext highlighter-rouge">feature</code> 브랜치에서 <code class="language-plaintext highlighter-rouge">develop</code> 브랜치로 PR을 생성한다고 하자. <code class="language-plaintext highlighter-rouge">feature</code>는 변경을 제공하는 source 또는 head branch이고, <code class="language-plaintext highlighter-rouge">develop</code>은 변경을 반영하는 target 또는 base branch다.</p>

<p>PR CI는 GitHub가 source와 target을 Merge한 테스트 결과를 Runner에서 검증하는 흐름이다. 충돌이 없는 경우 GitHub는 테스트용 Merge ref를 제공하고, Runner는 이를 기준으로 Build와 Test를 실행한다. 실제 target 브랜치는 이 과정에서 변경되지 않는다. <a href="https://docs.github.com/en/actions/reference/workflows-and-actions/events-that-trigger-workflows">Pull Request Event 문서</a></p>

<pre><code class="language-mermaid">sequenceDiagram
    participant C as Client
    participant G as GitHub
    participant R as Runner

    Note over C,R: PR Event
    C-&gt;&gt;G: commit push 및 Pull Request 생성 또는 갱신
    G-&gt;&gt;R: Workflow 실행
    R&lt;&lt;-&gt;&gt;G: fetch merge ref
    R-&gt;&gt;R: Build, Unit Test, Integration Test
    R--&gt;&gt;G: CI 결과 전달

    alt CI 실패
        C-&gt;&gt;C: 코드 수정
        C-&gt;&gt;G: 새 commit push
        Note over G,R: synchronize Event로 CI 재실행
    else CI 성공
        C-&gt;&gt;G: Review 및 Merge
    end
</code></pre>

<h5 id="특징">특징</h5>

<ul>
  <li>Build, Test, Lint, 분석을 주로 수행</li>
  <li>CI 실패 시 <code class="language-plaintext highlighter-rouge">Re-run failed jobs</code>로 재실행하거나 코드를 수정해 새 commit을 Push한다. 새 commit에는 <code class="language-plaintext highlighter-rouge">synchronize</code>가 발생한다.</li>
</ul>

<h5 id="draft-pr-의미">Draft PR 의미</h5>

<blockquote>
  <p>PR 상태 중 1을 의미</p>
</blockquote>

<p>Draft PR은 아직 정식 Review와 Merge 준비가 끝난 변경임을 표시하는 상태다.
Draft는 Merge할 수 없고, Code Owners에 대한 자동 Review 요청도 발생하지 않는다. 준비가 끝나면 <code class="language-plaintext highlighter-rouge">Ready for review</code>로 전환한다. <a href="https://docs.github.com/en/pull-requests/how-tos/create-pull-requests/changing-the-stage-of-a-pull-request">Pull Request 상태 변경</a></p>

<h5 id="pr-상태전이">PR 상태전이</h5>
<pre><code class="language-mermaid">stateDiagram-v2
    state "Draft" as PR_DRAFT
    state "Ready" as PR_READY
    state "Merged" as PR_MERGED
    state "Closed" as PR_CLOSED

    [*] --&gt; PR_DRAFT: 초안 생성

    PR_DRAFT --&gt; PR_READY: 리뷰 요청
    PR_READY --&gt; PR_DRAFT: 초안 전환

    PR_DRAFT --&gt; PR_CLOSED: 병합 없이 종료
    PR_READY --&gt; PR_CLOSED: 병합 없이 종료
    PR_READY --&gt; PR_MERGED: 병합

    PR_CLOSED --&gt; PR_DRAFT: 초안으로 재개
    PR_CLOSED --&gt; PR_READY: 리뷰 상태로 재개

    PR_MERGED --&gt; [*]
</code></pre>

<h4 id="push-flow">Push Flow</h4>

<p>PR을 통해 <code class="language-plaintext highlighter-rouge">source</code>의 내용이 <code class="language-plaintext highlighter-rouge">target</code>에 병합되면 <code class="language-plaintext highlighter-rouge">push</code> 이벤트를 통해 CI를 발동시킬 수 있다.
대개 대상은 <code class="language-plaintext highlighter-rouge">develop</code>이며, feature 내용이 PR승인되었을때 개발계에 자동배포를 위한 용도로 사용된다.</p>

<pre><code class="language-mermaid">sequenceDiagram
    participant C as Client
    participant G as GitHub
    participant R as Runner

    Note over C,R: Push Event
    G-&gt;&gt;R: 기본 브랜치 반영 후 Workflow 실행
    R&lt;&lt;-&gt;&gt;G: fetch develop
    R-&gt;&gt;R: Build 및 Test 실행
    R-&gt;&gt;R: Artifact 또는 Image 생성
    R-&gt;&gt;G: Artifact 또는 Image Push
    R--&gt;&gt;G: CI 결과 전달
</code></pre>

<h5 id="특징-1">특징</h5>

<ul>
  <li>Build 후 개발계 자동배포 (Production X)</li>
</ul>

<h4 id="workflow-dispatch-flow">Workflow Dispatch Flow</h4>

<p><code class="language-plaintext highlighter-rouge">workflow_dispatch</code>는 GitHub UI나 API를 통해 사용자가 Workflow를 <code class="language-plaintext highlighter-rouge">수동</code>으로 트리거하는 Event다.</p>

<pre><code class="language-mermaid">sequenceDiagram
    participant C as Client
    participant G as GitHub
    participant R as Runner

    Note over C,R: workflow_dispatch Event
    C-&gt;&gt;G: Workflow와 입력값, 대상 Ref 선택
    G-&gt;&gt;R: Workflow 실행
    R&lt;&lt;-&gt;&gt;G: fetch 대상 Ref
    R-&gt;&gt;R: 수동 작업 실행
    R--&gt;&gt;G: 실행 결과 전달
    G--&gt;&gt;C: 결과 표시
</code></pre>

<h5 id="특징-2">특징</h5>

<ul>
  <li>특정 릴리즈를 생성하거나 Production을 수동으로 배포하거나, 실패한 운영 작업을 재처리하는 데 사용한다.</li>
</ul>

<p>CI Flow를 Workflow YAML로 작성하는 방법은 <a href="/github-actions-workflow-writing.html">GitHub Actions Workflow 작성하기</a>에서 다룬다.</p>

<!-- ### 운영 기준 -->
<!-- --- -->
<!---->
<!-- - **실행 조건**: Pull Request에서 필요한 Check가 빠지지 않는지 확인한다. 경로 필터 때문에 필수 Check가 실행되지 않는 상태를 만들지 않는다. -->
<!-- - **재현성**: 런타임 버전과 의존성 설치 명령을 명시한다. Lock file이 있다면 `npm ci`처럼 Lock file을 기준으로 설치하는 명령을 사용한다. -->
<!-- - **책임 분리**: Build는 산출물 생성 가능 여부를, Test는 동작 검증을 담당한다. 한 명령에 모든 책임을 숨기지 않는다. -->
<!-- - **Merge 기준**: CI 성공만으로 품질 검증이 끝나는 것은 아니다. 저장소의 Review 규칙과 필수 Check를 함께 충족해야 한다. -->
<!-- - **Action 관리**: 사용하는 Action의 버전과 권한을 검토한다. 외부 Action은 변경될 수 있으므로 업데이트도 Workflow 변경으로 취급한다. -->
<!-- - **중복 실행**: Pull Request와 기본 브랜치의 `push`가 같은 Job을 실행하면 검증이 중복될 수 있다. Event별 목적과 비용을 확인한다. -->

<h3 id="conclusion">Conclusion</h3>
<hr />

<p>모던한 GitHub Actions CI Flow는 Pull Request 변경을 Event로 받아 Runner에서 일관된 환경을 준비하고, Build와 Test를 실행한 뒤 Check 결과를 Review와 Merge 판단에 제공한다.</p>

<h3 id="references">References</h3>
<hr />

<ul>
  <li><a href="https://docs.github.com/en/actions/get-started/understand-github-actions">Understanding GitHub Actions</a></li>
  <li><a href="https://docs.github.com/en/actions/concepts/workflows-and-actions/workflows">Workflows</a></li>
  <li><a href="https://docs.github.com/en/actions/reference/workflows-and-actions/events-that-trigger-workflows">Events that trigger workflows</a></li>
  <li><a href="https://docs.github.com/en/actions/tutorials/create-an-example-workflow">Creating an example workflow</a></li>
  <li><a href="https://docs.github.com/en/pull-requests/how-tos/create-pull-requests/changing-the-stage-of-a-pull-request">Changing the stage of a pull request</a></li>
</ul>]]></content><author><name>HwiRyung Kim</name></author><category term="infrastructure" /><summary type="html"><![CDATA[TOC]]></summary></entry><entry><title type="html">Hermes Agent를 Discord에 연결하기</title><link href="https://hrllk.github.io/hermes-discord-integration.html" rel="alternate" type="text/html" title="Hermes Agent를 Discord에 연결하기" /><published>2026-08-12T00:00:00+00:00</published><updated>2026-08-12T00:00:00+00:00</updated><id>https://hrllk.github.io/hermes-discord-integration</id><content type="html" xml:base="https://hrllk.github.io/hermes-discord-integration.html"><![CDATA[<h3 id="toc">TOC</h3>
<hr />
<ol>
  <li><a href="#overview">Overview</a></li>
  <li><a href="#로컬-gateway와-discord-연결-구조">로컬 Gateway와 Discord 연결 구조</a></li>
  <li><a href="#애플리케이션-설정">애플리케이션 설정</a></li>
  <li><a href="#게이트웨이-봇-설정-및-재시작">게이트웨이 봇 설정 및 재시작</a></li>
  <li><a href="#정리">정리</a></li>
</ol>

<h3 id="overview">Overview</h3>
<hr />

<p>Hermes Agent를 Discord 봇으로 연결하면 Discord DM이나 서버 채널에서 Hermes의 도구 사용, 메모리, 세션 기능을 그대로 사용할 수 있다.</p>

<p>이 글은 가장 작은 구성으로 연결하는 절차만 다룬다. 각 설정은 <strong>무엇을 하는지보다 왜 필요한지</strong>에 초점을 둔다.</p>

<h3 id="로컬-gateway와-discord-연결-구조">로컬 Gateway와 Discord 연결 구조</h3>
<hr />

<p>Hermes는 로컬에서 Gateway를 실행하는 방식으로 Discord와 연결한다. Gateway가 실행되면 Discord 서버와 연결을 시도하고, 연결이 성립된 뒤에는 그 상태를 유지한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>로컬 컴퓨터
    │
    │ Hermes Gateway 실행
    │
    ▼
Hermes Gateway ◀──── 연결 유지 ────▶ Discord 서버
    ▲                                  │
    │                                  │ 메시지 전달
    └──────── 모바일 Discord 클라이언트
</code></pre></div></div>

<p>모바일 Discord 클라이언트에서 메시지를 보내면 Discord 서버가 연결된 로컬 Gateway로 메시지를 전달한다. Gateway는 메시지를 Hermes에 전달하고, 처리 결과를 다시 Discord 서버로 보내 모바일 클라이언트에 표시한다.</p>

<p>핵심은 모바일 클라이언트가 로컬 컴퓨터에 직접 접속하는 것이 아니라, Discord 서버를 중계 지점으로 사용한다는 점이다. 로컬 Gateway와 Discord 서버의 연결이 유지되어야 Discord에서 보낸 요청을 로컬 Hermes가 받을 수 있다.</p>

<h3 id="애플리케이션-설정">애플리케이션 설정</h3>
<hr />

<p><a href="https://discord.com/developers/applications">Discord Developer Portal</a>에 로그인한 뒤 <code class="language-plaintext highlighter-rouge">New Application</code>을 선택한다.</p>

<p>애플리케이션은 봇의 컨테이너다. 먼저 애플리케이션을 만들고, 그 안에서 봇 사용자, 토큰, 권한, Gateway 연결을 설정한다.</p>

<h4 id="애플리케이션-생성">애플리케이션 생성</h4>

<p>애플리케이션 이름은 Discord에서 봇을 구분하기 위한 표시값이다. 용도를 바로 알 수 있는 이름을 사용하면 여러 봇을 관리하기 쉽다.</p>

<p><code class="language-plaintext highlighter-rouge">New Application</code>을 선택하고 봇을 식별할 이름을 입력한다.</p>

<h4 id="oauth-설정">OAuth 설정</h4>

<p>OAuth는 Discord 계정의 로그인 정보를 직접 전달하지 않고, 정해진 권한 범위로 봇을 서버에 설치하기 위한 인증·인가 방식이다. 여기서 봇이 요청할 기능과 서버 권한을 선택하면 Discord가 해당 설정을 반영한 초대 URL을 만들어준다.</p>

<h5 id="scope-설정">Scope 설정</h5>

<p>Scope는 OAuth 인증 과정에서 봇이 어떤 종류의 설치 권한을 요청할지 결정한다.</p>

<ul>
  <li>Scopes: <code class="language-plaintext highlighter-rouge">bot</code>, <code class="language-plaintext highlighter-rouge">applications.commands</code></li>
</ul>

<p><code class="language-plaintext highlighter-rouge">bot</code>은 서버에 봇 사용자를 설치하기 위한 scope다. <code class="language-plaintext highlighter-rouge">applications.commands</code>는 Hermes의 slash command를 Discord 명령어로 등록하기 위해 필요하다.</p>

<h5 id="permission-설정">Permission 설정</h5>

<p>Permission은 봇이 서버에서 수행할 수 있는 Discord 동작의 범위를 결정한다.</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">View Channels</code>: 봇이 메시지를 받을 채널을 볼 수 있어야 한다.</li>
  <li><code class="language-plaintext highlighter-rouge">Send Messages</code>: 답변을 보낼 수 있어야 한다.</li>
  <li><code class="language-plaintext highlighter-rouge">Read Message History</code>: 대화 맥락을 복원할 수 있어야 한다.</li>
  <li><code class="language-plaintext highlighter-rouge">Embed Links</code>: 링크와 결과를 Discord 형식으로 표시할 수 있다.</li>
  <li><code class="language-plaintext highlighter-rouge">Attach Files</code>: 이미지, 오디오, 파일 결과를 보낼 수 있다.</li>
  <li><code class="language-plaintext highlighter-rouge">Send Messages in Threads</code>: 자동 생성된 스레드에서 답변할 수 있다.</li>
  <li><code class="language-plaintext highlighter-rouge">Add Reactions</code>: 처리 중인 메시지에 확인 반응을 남길 수 있다.</li>
</ul>

<p>필요한 권한만 선택하는 이유는 봇이 침해되었을 때 피해 범위를 줄이기 위해서다. 서버 전체 관리자 권한을 부여할 필요는 없다.</p>

<ul>
  <li>Permissions: <code class="language-plaintext highlighter-rouge">View Channels</code>, <code class="language-plaintext highlighter-rouge">Send Messages</code>, <code class="language-plaintext highlighter-rouge">Embed Links</code>, <code class="language-plaintext highlighter-rouge">Attach Files</code>, <code class="language-plaintext highlighter-rouge">Read Message History</code></li>
  <li>권장: <code class="language-plaintext highlighter-rouge">Send Messages in Threads</code>, <code class="language-plaintext highlighter-rouge">Add Reactions</code></li>
</ul>

<h5 id="초대">초대</h5>

<p>앞에서 Scope와 Permission을 순서대로 설정했다면 Discord가 봇 초대 URL을 자동으로 만들어준다. 이 URL을 브라우저에 복사해 붙여넣으면 설정된 봇을 내 서버에 초대할 수 있다.</p>

<p>봇을 초대할 서버는 사전에 구성되어 있어야 하며, 초대하는 계정에는 해당 서버를 관리할 권한이 필요하다.</p>

<h4 id="bot-설정">Bot 설정</h4>

<p>봇은 Discord 서버와 실제로 통신하는 주체다. 애플리케이션만 만들고 봇을 설정하지 않으면 Hermes Gateway가 로그인할 대상이 없다.</p>

<p>왼쪽 메뉴에서 <code class="language-plaintext highlighter-rouge">Bot</code>으로 이동한다. Discord 애플리케이션에는 봇 사용자가 자동으로 생성된다.</p>

<h5 id="bot-token-발급">Bot Token 발급</h5>

<p><code class="language-plaintext highlighter-rouge">Token</code>에서 <code class="language-plaintext highlighter-rouge">Reset Token</code>을 눌러 토큰을 발급하고 즉시 안전한 곳에 복사한다.</p>

<h5 id="privileged-gateway-intents-설정">Privileged Gateway Intents 설정</h5>

<p>Gateway Intent는 Discord가 봇에게 전달할 이벤트의 범위를 결정한다. Hermes가 메시지와 발신자 정보를 처리하려면 다음 두 Intent가 필요하다.</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">Server Members Intent</code></li>
  <li><code class="language-plaintext highlighter-rouge">Message Content Intent</code></li>
</ul>

<p><code class="language-plaintext highlighter-rouge">Message Content Intent</code>는 봇이 실제 메시지 내용을 읽기 위해 필요하다. 이 설정이 꺼져 있으면 봇은 메시지 이벤트를 받아도 사용자가 입력한 텍스트를 볼 수 없다.</p>

<p><code class="language-plaintext highlighter-rouge">Server Members Intent</code>는 사용자와 멤버 정보를 확인하기 위해 필요하다. Hermes는 허용된 Discord 사용자 ID를 기준으로 접근을 제한하므로, 누가 메시지를 보냈는지 식별할 수 있어야 한다.</p>

<p><code class="language-plaintext highlighter-rouge">Presence Intent</code>는 온라인 상태를 확인하는 기능이며 기본적인 대화에는 필요하지 않다.</p>

<p>위 두 Intent를 켜고 <code class="language-plaintext highlighter-rouge">Save Changes</code>를 누른다.</p>

<h3 id="게이트웨이-봇-설정-및-재시작">게이트웨이 봇 설정 및 재시작</h3>
<hr />

<p>앞에서 Discord 서버에 애플리케이션과 봇에 대한 설정을 진행했다면, 이제 로컬에서 실행할 Hermes Gateway에 해당 봇의 인증 정보와 접근 정책을 설정한다. 설정이 끝나면 Gateway를 실행해 Discord와의 연결을 시작한다.</p>

<h4 id="사용자-id-확인하기">사용자 ID 확인하기</h4>

<p>Hermes는 허용된 Discord 사용자만 봇을 사용할 수 있도록 사용자 ID를 확인한다.</p>

<p>Discord에서 <code class="language-plaintext highlighter-rouge">Settings → Advanced → Developer Mode</code>를 켠 뒤 자신의 사용자 이름을 우클릭하고 <code class="language-plaintext highlighter-rouge">Copy User ID</code>를 선택한다.</p>

<h4 id="hermes-gateway-설정">Hermes Gateway 설정</h4>

<h5 id="대화방식">대화방식</h5>

<p>대화형 설정을 실행하고 Discord 봇 토큰과 사용자 ID를 입력한다. (숫자형태)</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>hermes gateway setup
</code></pre></div></div>

<h5 id="수동방식">수동방식</h5>

<p>수동 설정이 필요하면 <code class="language-plaintext highlighter-rouge">~/.hermes/.env</code>에 다음 값을 추가한다.</p>

<pre><code class="language-dotenv">DISCORD_BOT_TOKEN=your-bot-token
DISCORD_ALLOWED_USERS=your-discord-user-id
</code></pre>

<p>토큰은 외부에 노출되지 않도록 <code class="language-plaintext highlighter-rouge">.env</code>에 저장한다. 여러 사용자는 쉼표로 구분한다.</p>

<pre><code class="language-dotenv">DISCORD_ALLOWED_USERS=12938129...,192381923
</code></pre>

<h4 id="gateway-실행-및-재시작">Gateway 실행 및 재시작</h4>

<p>설정을 저장한 뒤 Gateway를 실행한다. 이미 Gateway가 실행 중이었다면 변경된 봇 설정을 읽도록 재시작해야 한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>hermes gateway
</code></pre></div></div>

<p>Gateway를 재시작하려면 실행 중인 프로세스를 종료한 뒤 다시 실행한다.</p>

<h3 id="정리">정리</h3>
<hr />

<ul>
  <li><a href="https://hermes-agent.nousresearch.com/docs/user-guide/messaging/discord">https://hermes-agent.nousresearch.com/docs/user-guide/messaging/discord</a></li>
</ul>]]></content><author><name>HwiRyung Kim</name></author><category term="infrastructure" /><summary type="html"><![CDATA[TOC Overview 로컬 Gateway와 Discord 연결 구조 애플리케이션 설정 게이트웨이 봇 설정 및 재시작 정리]]></summary></entry><entry><title type="html">Transactional Outbox in Practice: Operating in Production</title><link href="https://hrllk.github.io/transactional-outbox-in-practice-operating-in-production.html" rel="alternate" type="text/html" title="Transactional Outbox in Practice: Operating in Production" /><published>2026-04-27T00:00:00+00:00</published><updated>2026-04-27T00:00:00+00:00</updated><id>https://hrllk.github.io/transactional-outbox-in-practice-operating-in-production</id><content type="html" xml:base="https://hrllk.github.io/transactional-outbox-in-practice-operating-in-production.html"><![CDATA[<h3 id="toc">TOC</h3>
<hr />
<ol>
  <li><a href="#overview">Overview</a></li>
  <li><a href="#멀티-인스턴스와-중복-발행">멀티 인스턴스와 중복 발행</a></li>
  <li><a href="#운영-설정-예시">운영 설정 예시</a></li>
  <li><a href="#관측-지점">관측 지점</a></li>
  <li><a href="#dlt-정책">DLT 정책</a></li>
  <li><a href="#conclusion">Conclusion</a></li>
</ol>

<h3 id="overview">Overview</h3>
<hr />
<p>앞선 글들에서 producer, poller, consumer의 기본 책임을 나눴다. 운영 환경에서는 여기에 멀티 인스턴스, 오래 쌓인 row, 컨슈머 실패 같은 현실 문제가 더해진다.</p>

<p>이번 글은 Outbox를 운영할 때 반드시 정해야 하는 몇 가지 기준을 짧게 정리한다.</p>

<h3 id="멀티-인스턴스와-중복-발행">멀티 인스턴스와 중복 발행</h3>
<hr />
<p>애플리케이션 인스턴스가 다중화되면 각 인스턴스의 폴러가 같은 PENDING row를 동시에 읽을 수 있다.</p>

<p>우리는 폴 쿼리에 <code class="language-plaintext highlighter-rouge">SELECT ... FOR UPDATE SKIP LOCKED</code>를 기본으로 둬 경합을 방지한다. 그래도 PROCESSING 도중 인스턴스가 죽는 시나리오는 남기 때문에, 발행 중복 자체를 0으로 만들기보다 멱등 컨슈머가 흡수하는 모델을 유지한다.</p>

<div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">-- Outbox 폴러가 PENDING row를 클레임하는 쿼리 패턴</span>
<span class="k">SELECT</span> <span class="o">*</span> <span class="k">FROM</span> <span class="n">TB_TRB_OX_001</span>
 <span class="k">WHERE</span> <span class="n">STATUS</span> <span class="o">=</span> <span class="s1">'PENDING'</span>
 <span class="k">ORDER</span> <span class="k">BY</span> <span class="n">CREATED_AT</span>
 <span class="k">LIMIT</span> <span class="mi">50</span>
   <span class="k">FOR</span> <span class="k">UPDATE</span> <span class="n">SKIP</span> <span class="n">LOCKED</span><span class="p">;</span>
<span class="c1">-- SKIP LOCKED: 다른 인스턴스가 이미 잠근 row는 자동으로 건너뜀</span>
</code></pre></div></div>

<h3 id="운영-설정-예시">운영 설정 예시</h3>
<hr />
<p>message-lib이 제공하는 주요 설정값을 정리한다. 폴러 동작 튜닝과 SENT row 보관 정책이 핵심이다.</p>

<p>SENT row를 무한히 보관하면 폴링 쿼리와 테이블 관리 비용이 커진다. PENDING/PROCESSING/DEAD row는 정리 대상에서 제외하는 것이 원칙이다. 발행되지 않은 row는 아직 시스템이 처리해야 할 의도이고, DEAD는 운영자가 들여다봐야 할 신호이기 때문이다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">outbox</span><span class="pi">:</span>
  <span class="c1"># 폴러 동작</span>
  <span class="na">poll-interval-ms</span><span class="pi">:</span> <span class="m">500</span>         <span class="c1"># 발행 latency 상한 (ms)</span>
  <span class="na">batch-size</span><span class="pi">:</span> <span class="m">50</span>                <span class="c1"># 한 폴 사이클에 처리할 최대 PENDING row 수</span>
  <span class="na">max-retries</span><span class="pi">:</span> <span class="m">3</span>                <span class="c1"># 재시도 초과 시 DEAD 전환</span>

  <span class="c1"># Row 보관</span>
  <span class="na">cleanup-retention-days</span><span class="pi">:</span> <span class="m">30</span>    <span class="c1"># SENT row 보관 기간 (이후 삭제)</span>
  <span class="na">cleanup-cron</span><span class="pi">:</span> <span class="s2">"</span><span class="s">0</span><span class="nv"> </span><span class="s">0</span><span class="nv"> </span><span class="s">3</span><span class="nv"> </span><span class="s">*</span><span class="nv"> </span><span class="s">*</span><span class="nv"> </span><span class="s">*"</span>   <span class="c1"># 매일 새벽 3시 실행</span>
  <span class="c1"># PENDING / PROCESSING / DEAD 는 정리 대상에서 제외</span>
</code></pre></div></div>

<h3 id="관측-지점">관측 지점</h3>
<hr />
<p>Outbox가 정상 동작하는지는 코드만으로는 알 수 없다. 발행 파이프라인이 살아 있는지는 메트릭으로 확인해야 한다.</p>

<p>이 시리즈에서 Outbox 폴러·발행을 담당하는 공용 SDK인 <code class="language-plaintext highlighter-rouge">message-lib</code>은 폴링·재시도·DEAD 전환을 내부에서 처리하며, 4개 메트릭을 기본으로 노출한다.</p>

<table>
  <thead>
    <tr>
      <th>메트릭</th>
      <th>타입</th>
      <th>의미</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">outbox.events.published</code></td>
      <td>Counter</td>
      <td>Kafka 발행 성공 횟수</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">outbox.events.failed</code></td>
      <td>Counter</td>
      <td>Kafka 발행 실패 횟수</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">outbox.events.dead</code></td>
      <td>Counter</td>
      <td>DEAD 상태 전환 횟수</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">outbox.queue.pending</code></td>
      <td>Gauge</td>
      <td>현재 미발행 PENDING row 수</td>
    </tr>
  </tbody>
</table>

<p>운영에서 먼저 봐야 하는 두 값은 <code class="language-plaintext highlighter-rouge">outbox.queue.pending</code>과 <code class="language-plaintext highlighter-rouge">outbox.events.dead</code>다. <code class="language-plaintext highlighter-rouge">pending</code>이 늘어나면 폴러가 처리 속도를 따라가지 못하거나 멈췄다는 신호다. <code class="language-plaintext highlighter-rouge">dead</code>가 발생하면 Consumer가 끝내 처리하지 못한 메시지가 있다는 뜻이며, 이는 다음 섹션의 DLT 정책과 직접 연결된다.</p>

<h3 id="dlt-정책">DLT 정책</h3>
<hr />
<p>재시도를 모두 소진한 메시지가 DLT에 도착했을 때의 정책도 미리 정해야 한다.</p>

<p>대부분의 경우 상세 상태를 <code class="language-plaintext highlighter-rouge">FAIL</code>로 마감하고 운영자 알림을 보내는 방식이 적합하다. 로깅만 하고 상태를 남겨두면 사용자 입장에서는 작업이 영원히 진행 중인 것처럼 보일 수 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@DltHandler</span>
<span class="kd">public</span> <span class="kt">void</span> <span class="nf">onDlt</span><span class="o">(</span><span class="nc">ConsumerRecord</span><span class="o">&lt;</span><span class="nc">String</span><span class="o">,</span> <span class="nc">UserRegistrationEventAvro</span><span class="o">&gt;</span> <span class="n">record</span><span class="o">)</span> <span class="o">{</span>
    <span class="nc">UserRegistrationEventAvro</span> <span class="n">event</span> <span class="o">=</span> <span class="n">record</span><span class="o">.</span><span class="na">value</span><span class="o">();</span>
    <span class="nc">String</span> <span class="n">dtlId</span> <span class="o">=</span> <span class="n">event</span> <span class="o">!=</span> <span class="kc">null</span> <span class="o">?</span> <span class="n">event</span><span class="o">.</span><span class="na">getUserRegDmndDtlId</span><span class="o">().</span><span class="na">toString</span><span class="o">()</span> <span class="o">:</span> <span class="s">"&lt;unknown&gt;"</span><span class="o">;</span>
    <span class="n">log</span><span class="o">.</span><span class="na">error</span><span class="o">(</span><span class="s">"[UserRegistration] DLT received: dtlId={}"</span><span class="o">,</span> <span class="n">dtlId</span><span class="o">);</span>

    <span class="c1">// ① 상세 상태를 FAIL로 마감 — 사용자에게 "처리 실패"로 노출</span>
    <span class="n">consumeUseCase</span><span class="o">.</span><span class="na">markAsDeadLetter</span><span class="o">(</span><span class="n">dtlId</span><span class="o">,</span> <span class="s">"max retries exhausted"</span><span class="o">);</span>

    <span class="c1">// ② 운영자 알림 (Slack / Email 등) — 알림 채널은 팀 인프라에 맞게 연결</span>
<span class="o">}</span>
</code></pre></div></div>

<h3 id="conclusion">Conclusion</h3>
<hr />
<p>Transactional Outbox는 exactly-once를 공짜로 주는 패턴이 아니다. 분산 트랜잭션의 부담을 <strong>발행 재시도, 멱등 소비, 운영 관측</strong>이라는 더 다루기 쉬운 책임으로 나누는 패턴이다.</p>

<p>이 시리즈의 결론은 단순하다. 저장과 발행 의도를 원자적으로 묶고, 발행은 at-least-once로 받아들이며, 컨슈머와 운영 도구가 중복과 누락을 흡수하게 만든다.</p>]]></content><author><name>HwiRyung Kim</name></author><category term="design" /><summary type="html"><![CDATA[TOC Overview 멀티 인스턴스와 중복 발행 운영 설정 예시 관측 지점 DLT 정책 Conclusion]]></summary></entry><entry><title type="html">Transactional Outbox in Practice: Idempotent Consumer</title><link href="https://hrllk.github.io/transactional-outbox-in-practice-idempotent-consumer.html" rel="alternate" type="text/html" title="Transactional Outbox in Practice: Idempotent Consumer" /><published>2026-04-25T00:00:00+00:00</published><updated>2026-04-25T00:00:00+00:00</updated><id>https://hrllk.github.io/transactional-outbox-in-practice-idempotent-consumer</id><content type="html" xml:base="https://hrllk.github.io/transactional-outbox-in-practice-idempotent-consumer.html"><![CDATA[<h3 id="toc">TOC</h3>
<hr />
<ol>
  <li><a href="#overview">Overview</a></li>
  <li><a href="#consumer의-위치">Consumer의 위치</a></li>
  <li><a href="#멱등-상태-머신">멱등 상태 머신</a></li>
  <li><a href="#race-condition-대응">Race Condition 대응</a></li>
  <li><a href="#외부-api와-상태-전이-분리">외부 API와 상태 전이 분리</a></li>
  <li><a href="#conclusion">Conclusion</a></li>
  <li><a href="#next-step">Next Step</a></li>
</ol>

<h3 id="overview">Overview</h3>
<hr />
<p>이전 글에서 Outbox 발행은 at-least-once라고 정리했다. 즉, 같은 메시지가 두 번 도착할 수 있고 컨슈머는 이를 정상 상황으로 받아들여야 한다.</p>

<p>이번 글에서는 중복 메시지가 와도 비즈니스 상태가 흔들리지 않도록 만드는 <strong>멱등 컨슈머</strong>의 기준을 정리한다.</p>

<h3 id="consumer의-위치">Consumer의 위치</h3>
<hr />
<p>Kafka consumer는 외부 시스템에서 들어오는 입력을 application use case로 전달하는 inbound adapter다 — 외부 메시지를 내부 유스케이스 호출로 변환하는 역할.</p>

<p>메시지를 역직렬화하고 use case를 호출하되, 상태 전이와 정합성 판단은 application/domain 쪽에 둔다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Component</span>
<span class="nd">@RequiredArgsConstructor</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">GitLabUserRegistrationConsumer</span> <span class="o">{</span>

    <span class="kd">private</span> <span class="kd">final</span> <span class="nc">GitLabUserRegistrationConsumeUseCase</span> <span class="n">consumeUseCase</span><span class="o">;</span>

    <span class="nd">@RetryableTopic</span><span class="o">(</span><span class="n">attempts</span> <span class="o">=</span> <span class="s">"3"</span><span class="o">,</span> <span class="n">backoff</span> <span class="o">=</span> <span class="nd">@Backoff</span><span class="o">(</span><span class="n">delay</span> <span class="o">=</span> <span class="mi">1000</span><span class="o">,</span> <span class="n">multiplier</span> <span class="o">=</span> <span class="mf">2.0</span><span class="o">))</span>
    <span class="nd">@KafkaListener</span><span class="o">(</span><span class="n">topics</span> <span class="o">=</span> <span class="s">"#{T(Topics).GITLAB_USER_REG_COMMAND.getValue()}"</span><span class="o">)</span>
    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">onMessage</span><span class="o">(</span><span class="nd">@Payload</span> <span class="nc">UserRegistrationEventAvro</span> <span class="n">event</span><span class="o">)</span> <span class="o">{</span>
        <span class="n">consumeUseCase</span><span class="o">.</span><span class="na">processRegistrationEvent</span><span class="o">(</span><span class="n">event</span><span class="o">);</span>   <span class="c1">// 상태 전이 책임은 UseCase에</span>
    <span class="o">}</span>

    <span class="nd">@DltHandler</span>
    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">onDlt</span><span class="o">(</span><span class="nc">ConsumerRecord</span><span class="o">&lt;</span><span class="nc">String</span><span class="o">,</span> <span class="nc">UserRegistrationEventAvro</span><span class="o">&gt;</span> <span class="n">record</span><span class="o">)</span> <span class="o">{</span>
        <span class="nc">String</span> <span class="n">dtlId</span> <span class="o">=</span> <span class="n">record</span><span class="o">.</span><span class="na">value</span><span class="o">().</span><span class="na">getUserRegDmndDtlId</span><span class="o">().</span><span class="na">toString</span><span class="o">();</span>
        <span class="n">consumeUseCase</span><span class="o">.</span><span class="na">markAsDeadLetter</span><span class="o">(</span><span class="n">dtlId</span><span class="o">,</span> <span class="s">"max retries exhausted"</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<h3 id="멱등-상태-머신">멱등 상태 머신</h3>
<hr />
<p>상세 처리 상태를 <code class="language-plaintext highlighter-rouge">WAIT → EXCN → SCS/FAIL</code>처럼 명확한 상태 머신으로 둔다.</p>

<p>핵심 규칙은 단순하다. <code class="language-plaintext highlighter-rouge">SCS</code>와 <code class="language-plaintext highlighter-rouge">FAIL</code>은 종결 상태이며, 종결된 row는 같은 메시지가 다시 와도 더 이상 상태를 바꾸지 않는다. 상태 전이 메서드(<code class="language-plaintext highlighter-rouge">markInProgress</code>, <code class="language-plaintext highlighter-rouge">markSuccess</code>, <code class="language-plaintext highlighter-rouge">markFailure</code>)는 모두 종결 상태에서 들어온 호출을 silent skip하도록 짠다. 컨슈머 진입 직후 <code class="language-plaintext highlighter-rouge">isTerminated</code> early skip까지 더하면 같은 메시지의 재처리는 자연스럽게 흡수된다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// 상세 처리 상태 enum</span>
<span class="kd">public</span> <span class="kd">enum</span> <span class="nc">GitLabUserRegistrationDetailStatus</span> <span class="o">{</span>
    <span class="no">WAIT</span><span class="o">,</span> <span class="no">EXCN</span><span class="o">,</span> <span class="no">SCS</span><span class="o">,</span> <span class="no">FAIL</span><span class="o">;</span>

    <span class="kd">public</span> <span class="kt">boolean</span> <span class="nf">isTerminated</span><span class="o">()</span> <span class="o">{</span>
        <span class="k">return</span> <span class="k">this</span> <span class="o">==</span> <span class="no">SCS</span> <span class="o">||</span> <span class="k">this</span> <span class="o">==</span> <span class="no">FAIL</span><span class="o">;</span>
    <span class="o">}</span>
<span class="o">}</span>

<span class="c1">// 엔티티 도메인 메서드 — 종결 상태에서 재전이는 silent skip</span>
<span class="kd">public</span> <span class="nc">GitLabUserRegistrationDetailStatus</span> <span class="nf">current</span><span class="o">()</span> <span class="o">{</span>
    <span class="k">return</span> <span class="nc">GitLabUserRegistrationDetailStatus</span><span class="o">.</span><span class="na">from</span><span class="o">(</span><span class="k">this</span><span class="o">.</span><span class="na">prgrsSttsCd</span><span class="o">);</span>
<span class="o">}</span>

<span class="kd">public</span> <span class="kt">boolean</span> <span class="nf">isTerminated</span><span class="o">()</span> <span class="o">{</span>
    <span class="k">return</span> <span class="nf">current</span><span class="o">().</span><span class="na">isTerminated</span><span class="o">();</span>
<span class="o">}</span>

<span class="kd">public</span> <span class="kt">void</span> <span class="nf">markInProgress</span><span class="o">()</span> <span class="o">{</span>
    <span class="k">if</span> <span class="o">(</span><span class="n">isTerminated</span><span class="o">()</span> <span class="o">||</span> <span class="n">current</span><span class="o">()</span> <span class="o">==</span> <span class="no">EXCN</span><span class="o">)</span> <span class="k">return</span><span class="o">;</span>
    <span class="k">this</span><span class="o">.</span><span class="na">prgrsSttsCd</span> <span class="o">=</span> <span class="no">EXCN</span><span class="o">.</span><span class="na">getCode</span><span class="o">();</span>
    <span class="k">this</span><span class="o">.</span><span class="na">rsn</span> <span class="o">=</span> <span class="kc">null</span><span class="o">;</span>
<span class="o">}</span>

<span class="kd">public</span> <span class="kt">void</span> <span class="nf">markSuccess</span><span class="o">(</span><span class="nc">String</span> <span class="n">msg</span><span class="o">)</span> <span class="o">{</span>
    <span class="k">if</span> <span class="o">(</span><span class="n">isTerminated</span><span class="o">())</span> <span class="k">return</span><span class="o">;</span>
    <span class="k">this</span><span class="o">.</span><span class="na">prgrsSttsCd</span> <span class="o">=</span> <span class="no">SCS</span><span class="o">.</span><span class="na">getCode</span><span class="o">();</span>
    <span class="k">this</span><span class="o">.</span><span class="na">rsn</span> <span class="o">=</span> <span class="n">msg</span><span class="o">;</span>
<span class="o">}</span>

<span class="kd">public</span> <span class="kt">void</span> <span class="nf">markFailure</span><span class="o">(</span><span class="nc">String</span> <span class="n">msg</span><span class="o">)</span> <span class="o">{</span>
    <span class="k">if</span> <span class="o">(</span><span class="n">isTerminated</span><span class="o">())</span> <span class="k">return</span><span class="o">;</span>
    <span class="k">this</span><span class="o">.</span><span class="na">prgrsSttsCd</span> <span class="o">=</span> <span class="no">FAIL</span><span class="o">.</span><span class="na">getCode</span><span class="o">();</span>
    <span class="k">this</span><span class="o">.</span><span class="na">rsn</span> <span class="o">=</span> <span class="n">msg</span><span class="o">;</span>
<span class="o">}</span>
</code></pre></div></div>

<h3 id="race-condition-대응">Race Condition 대응</h3>
<hr />
<p>중복 메시지가 거의 동시에 도착할 수 있는데, 우리는 <code class="language-plaintext highlighter-rouge">@Version</code> 같은 낙관락을 따로 두지 않는다. 운영 중인 테이블에 컬럼을 추가하는 DDL ALTER 비용을 피하고, 대신 다음 세 가지로 race를 흡수한다.</p>

<ol>
  <li>Kafka 파티션 정합성 — 메시지 key를 상세 ID로 고정하면 같은 상세는 항상 같은 파티션, 같은 컨슈머 스레드에서 직렬로 처리된다.</li>
  <li>컨슈머 진입 시 <code class="language-plaintext highlighter-rouge">isTerminated</code> early skip — 종결된 건은 비즈니스 로직 자체에 들어가지 않는다.</li>
  <li>멱등한 상태 전이 메서드 — 들어왔다 하더라도 종결 상태에서는 no-op.</li>
</ol>

<p>다중 인스턴스 리밸런싱 도중 짧게 겹치는 시나리오는 위 3중 가드로 흡수된다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Override</span>
<span class="kd">public</span> <span class="kt">void</span> <span class="nf">processRegistrationEvent</span><span class="o">(</span><span class="nc">UserRegistrationEventAvro</span> <span class="n">event</span><span class="o">)</span> <span class="o">{</span>
    <span class="nc">GitLabUserRegistrationDetailId</span> <span class="n">id</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">GitLabUserRegistrationDetailId</span><span class="o">(</span>
            <span class="n">event</span><span class="o">.</span><span class="na">getUserRegDmndId</span><span class="o">().</span><span class="na">toString</span><span class="o">(),</span>
            <span class="n">event</span><span class="o">.</span><span class="na">getUserRegDmndDtlId</span><span class="o">().</span><span class="na">toString</span><span class="o">());</span>

    <span class="c1">// ① 멱등 가드 — 이미 종결된 건은 여기서 끊는다</span>
    <span class="k">if</span> <span class="o">(</span><span class="n">queryPort</span><span class="o">.</span><span class="na">isTerminated</span><span class="o">(</span><span class="n">id</span><span class="o">))</span> <span class="o">{</span>
        <span class="k">return</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="c1">// ② EXCN 으로 전이 (REQUIRES_NEW — 짧은 트랜잭션)</span>
    <span class="n">commandPort</span><span class="o">.</span><span class="na">markInProgress</span><span class="o">(</span><span class="n">id</span><span class="o">);</span>

    <span class="k">try</span> <span class="o">{</span>
        <span class="c1">// ③ 외부 API 호출 (트랜잭션 밖)</span>
        <span class="n">provisionPort</span><span class="o">.</span><span class="na">registerUser</span><span class="o">(</span><span class="n">event</span><span class="o">.</span><span class="na">getToolId</span><span class="o">().</span><span class="na">toString</span><span class="o">(),</span> <span class="n">event</span><span class="o">.</span><span class="na">getTargetUserId</span><span class="o">().</span><span class="na">toString</span><span class="o">());</span>
        <span class="n">commandPort</span><span class="o">.</span><span class="na">markSuccess</span><span class="o">(</span><span class="n">id</span><span class="o">,</span> <span class="s">"정상 등록 완료"</span><span class="o">);</span>    <span class="c1">// REQUIRES_NEW</span>
    <span class="o">}</span> <span class="k">catch</span> <span class="o">(</span><span class="nc">Exception</span> <span class="n">e</span><span class="o">)</span> <span class="o">{</span>
        <span class="n">commandPort</span><span class="o">.</span><span class="na">markFailure</span><span class="o">(</span><span class="n">id</span><span class="o">,</span> <span class="n">e</span><span class="o">.</span><span class="na">getMessage</span><span class="o">());</span>      <span class="c1">// REQUIRES_NEW</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<h3 id="외부-api와-상태-전이-분리">외부 API와 상태 전이 분리</h3>
<hr />
<p>외부 API 호출이 오래 걸리는 동안 DB 트랜잭션을 붙잡고 있으면 진행 상태가 보이지 않고 락 유지 시간도 길어진다.</p>

<p><code class="language-plaintext highlighter-rouge">markInProgress</code>, <code class="language-plaintext highlighter-rouge">markSuccess</code>, <code class="language-plaintext highlighter-rouge">markFailure</code> 같은 상태 전이는 짧은 트랜잭션으로 분리하는 편이 운영상 유리하다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// 영속 어댑터 — 상태 전이마다 REQUIRES_NEW로 별도 커밋</span>
<span class="nd">@Override</span>
<span class="nd">@Transactional</span><span class="o">(</span><span class="n">propagation</span> <span class="o">=</span> <span class="nc">Propagation</span><span class="o">.</span><span class="na">REQUIRES_NEW</span><span class="o">)</span>
<span class="kd">public</span> <span class="kt">void</span> <span class="nf">markInProgress</span><span class="o">(</span><span class="nc">GitLabUserRegistrationDetailId</span> <span class="n">id</span><span class="o">)</span> <span class="o">{</span>
    <span class="n">loadDetail</span><span class="o">(</span><span class="n">id</span><span class="o">).</span><span class="na">markInProgress</span><span class="o">();</span>         <span class="c1">// findById → 도메인 메서드 → dirty checking flush</span>
    <span class="n">refreshMasterStatus</span><span class="o">(</span><span class="n">id</span><span class="o">.</span><span class="na">getUserRegDmndId</span><span class="o">());</span>
<span class="o">}</span>

<span class="nd">@Override</span>
<span class="nd">@Transactional</span><span class="o">(</span><span class="n">propagation</span> <span class="o">=</span> <span class="nc">Propagation</span><span class="o">.</span><span class="na">REQUIRES_NEW</span><span class="o">)</span>
<span class="kd">public</span> <span class="kt">void</span> <span class="nf">markSuccess</span><span class="o">(</span><span class="nc">GitLabUserRegistrationDetailId</span> <span class="n">id</span><span class="o">,</span> <span class="nc">String</span> <span class="n">msg</span><span class="o">)</span> <span class="o">{</span>
    <span class="n">loadDetail</span><span class="o">(</span><span class="n">id</span><span class="o">).</span><span class="na">markSuccess</span><span class="o">(</span><span class="n">msg</span><span class="o">);</span>
    <span class="n">refreshMasterStatus</span><span class="o">(</span><span class="n">id</span><span class="o">.</span><span class="na">getUserRegDmndId</span><span class="o">());</span>
<span class="o">}</span>
</code></pre></div></div>

<h3 id="conclusion">Conclusion</h3>
<hr />
<p>멱등 컨슈머의 목적은 중복을 막는 것이 아니라 <strong>중복이 와도 결과가 같게 만드는 것</strong>이다. 이 원칙이 있어야 Outbox의 at-least-once 발행을 현실적인 설계로 받아들일 수 있다.</p>

<h3 id="next-step">Next Step</h3>
<hr />
<p>다음 포스트에서는 <strong><a href="/transactional-outbox-in-practice-operating-in-production.html">Operating in Production</a></strong>을 다룬다. 멀티 인스턴스, row retention, DLT 같은 운영 이슈를 정리한다.</p>]]></content><author><name>HwiRyung Kim</name></author><category term="design" /><summary type="html"><![CDATA[TOC Overview Consumer의 위치 멱등 상태 머신 Race Condition 대응 외부 API와 상태 전이 분리 Conclusion Next Step]]></summary></entry><entry><title type="html">Transactional Outbox in Practice: Thin Poller and At-least-once</title><link href="https://hrllk.github.io/transactional-outbox-in-practice-thin-poller-and-at-least-once.html" rel="alternate" type="text/html" title="Transactional Outbox in Practice: Thin Poller and At-least-once" /><published>2026-04-23T00:00:00+00:00</published><updated>2026-04-23T00:00:00+00:00</updated><id>https://hrllk.github.io/transactional-outbox-in-practice-thin-poller-and-at-least-once</id><content type="html" xml:base="https://hrllk.github.io/transactional-outbox-in-practice-thin-poller-and-at-least-once.html"><![CDATA[<h3 id="toc">TOC</h3>
<hr />
<ol>
  <li><a href="#overview">Overview</a></li>
  <li><a href="#thin-poller의-역할">Thin Poller의 역할</a></li>
  <li><a href="#발행과-마킹의-순서">발행과 마킹의 순서</a></li>
  <li><a href="#at-least-once-수용">At-least-once 수용</a></li>
  <li><a href="#운영-관점">운영 관점</a></li>
  <li><a href="#conclusion">Conclusion</a></li>
  <li><a href="#next-step">Next Step</a></li>
</ol>

<h3 id="overview">Overview</h3>
<hr />
<p>이전 글에서 비즈니스 데이터와 발행 의도를 Outbox table에 안전하게 저장했다. 이제 남은 일은 그 row를 Kafka로 보내고, 성공한 row를 발행 완료로 표시하는 것이다.</p>

<p>이번 글의 핵심은 폴러를 얇게 유지하는 것이다. 폴러는 비즈니스 로직을 처리하지 않고, <strong>미발행 row를 가져와 발행하고 마킹하는 일</strong>만 맡는다.</p>

<h3 id="thin-poller의-역할">Thin Poller의 역할</h3>
<hr />
<p>폴러가 외부 API 호출, 상태 전이, 실패 분류까지 떠안으면 거대한 배치 잡이 된다.</p>

<p>Outbox 폴러는 인프라성 컴포넌트로 두고, 실제 비즈니스 처리는 Kafka 컨슈머에게 넘긴다. 그래야 발행 실패와 소비 실패를 서로 다른 지점에서 관측하고 재시도할 수 있다.</p>

<h3 id="발행과-마킹의-순서">발행과 마킹의 순서</h3>
<hr />
<p>폴러는 row를 <code class="language-plaintext highlighter-rouge">PENDING</code>에서 <code class="language-plaintext highlighter-rouge">PROCESSING</code>으로 클레임한 뒤 Kafka send를 시도하고, send가 성공한 시점에 <code class="language-plaintext highlighter-rouge">SENT</code>로 전환한다. 실패하면 재시도 카운트를 올리거나 일정 횟수를 넘기면 <code class="language-plaintext highlighter-rouge">DEAD</code>로 분리한다. send를 먼저 했다고 가정하고 row를 미리 <code class="language-plaintext highlighter-rouge">SENT</code>로 바꾸면, send 실패 시 row가 이미 발행된 것처럼 보이기 때문이다.</p>

<p>하지만 send 성공 직후 <code class="language-plaintext highlighter-rouge">SENT</code> 전환 직전에 애플리케이션이 죽으면 같은 <code class="language-plaintext highlighter-rouge">PROCESSING</code> row가 다음 폴링에서 다시 발행될 수 있다. 이것이 Outbox가 기본적으로 <strong>at-least-once</strong>인 이유다.</p>

<details>
<summary><strong>at-least-once 전달 보장이란?</strong></summary>
<br />

메시지 전달 보장에는 세 가지 수준이 있다.

| 보장 수준 | 의미 | 중복 가능 | 유실 가능 |
|---|---|---|---|
| **at-most-once** | 최대 한 번 | 없음 | 있음 |
| **at-least-once** | 최소 한 번 | 있음 | 없음 |
| **exactly-once** | 정확히 한 번 | 없음 | 없음 |

Outbox 폴러가 at-least-once인 이유는 **`PROCESSING` → `SENT` 전환 사이에 윈도우**가 있기 때문이다.

```
① PENDING → PROCESSING  (클레임)
② Kafka send 성공
③ ← 이 순간 애플리케이션이 죽으면 row는 여전히 PROCESSING
④ 재시작된 폴러가 같은 row를 다시 발행
```

유실 없는 도달을 보장하는 대신, 중복 도달 가능성을 수용한다. exactly-once를 구현하려면 분산 트랜잭션(2PC) 같은 훨씬 무거운 프로토콜이 필요해지며, 그 비용이 실익보다 크다. 이 시리즈는 **중복을 Consumer가 흡수하는 설계**로 at-least-once를 실용적으로 처리한다.

</details>

<h3 id="at-least-once-수용">At-least-once 수용</h3>
<hr />
<p>이 시리즈에서는 그 비용을 치르지 않고, 중복을 컨슈머가 흡수하는 방향을 선택한다.</p>

<p>따라서 message key는 같은 대상이 같은 파티션으로 가도록 신중히 잡아야 한다. 예를 들어 상세 ID를 key로 쓰면 같은 상세에 대한 중복 메시지는 같은 순서 안에서 처리될 가능성이 높아진다. 같은 파티션에서 직렬로 도착해야 멱등 가드가 제대로 작동하는 이유는 3편 Race Condition 대응에서 자세히 다룬다.</p>

<h3 id="운영-관점">운영 관점</h3>
<hr />
<p>폴 주기는 latency와 DB 부하 사이의 trade-off다. 짧게 잡으면 발행 지연은 줄지만 빈 SELECT가 늘고, 길게 잡으면 DB 부담은 줄지만 메시지 도착이 늦어진다.</p>

<p>운영에서는 미발행 row 수와 발행 지연 시간을 함께 봐야 한다. 이 두 값이 Outbox 폴러의 건강 상태를 가장 직접적으로 보여준다.</p>

<h3 id="conclusion">Conclusion</h3>
<hr />
<p>이 단계에서 보장되는 것은 발행 책임의 분리다. 저장은 producer가, 발행은 폴러가, 중복 처리는 consumer가 맡는다.</p>

<h3 id="next-step">Next Step</h3>
<hr />
<p>다음 포스트에서는 <strong><a href="/transactional-outbox-in-practice-idempotent-consumer.html">Idempotent Consumer</a></strong>를 다룬다. at-least-once로 도착한 메시지를 어떻게 안전하게 소비할지 정리한다.</p>]]></content><author><name>HwiRyung Kim</name></author><category term="design" /><summary type="html"><![CDATA[TOC Overview Thin Poller의 역할 발행과 마킹의 순서 At-least-once 수용 운영 관점 Conclusion Next Step]]></summary></entry><entry><title type="html">Transactional Outbox in Practice: Atomic Persist and Enqueue</title><link href="https://hrllk.github.io/transactional-outbox-in-practice-atomic-persist-and-enqueue.html" rel="alternate" type="text/html" title="Transactional Outbox in Practice: Atomic Persist and Enqueue" /><published>2026-04-21T00:00:00+00:00</published><updated>2026-04-21T00:00:00+00:00</updated><id>https://hrllk.github.io/transactional-outbox-in-practice-atomic-persist-and-enqueue</id><content type="html" xml:base="https://hrllk.github.io/transactional-outbox-in-practice-atomic-persist-and-enqueue.html"><![CDATA[<h3 id="toc">TOC</h3>
<hr />
<ol>
  <li><a href="#overview">Overview</a></li>
  <li><a href="#application-service의-책임">Application Service의 책임</a></li>
  <li><a href="#outgoing-port-분리">Outgoing Port 분리</a></li>
  <li><a href="#outbox-row의-의미">Outbox Row의 의미</a></li>
  <li><a href="#주의할-점">주의할 점</a></li>
  <li><a href="#conclusion">Conclusion</a></li>
  <li><a href="#next-step">Next Step</a></li>
</ol>

<h3 id="overview">Overview</h3>
<hr />
<p>이전 글에서는 DB 저장과 MQ 발행을 동시에 성공시키기 어렵다는 문제를 정리했다. 이번 글에서는 그 첫 번째 해법으로 <strong>비즈니스 데이터와 Outbox row를 같은 트랜잭션에 저장하는 구조</strong>를 다룬다.</p>

<p>중요한 점은 application service가 Kafka를 직접 호출하지 않는다는 것이다. 이 계층은 “저장할 데이터”와 “발행할 의도”만 함께 남긴다.</p>

<h3 id="application-service의-책임">Application Service의 책임</h3>
<hr />
<p>Producer 측 application service는 요청을 검증하고, 비즈니스 데이터를 여러 테이블에 저장하고, 상세 단위 이벤트를 Outbox에 적재한다.</p>

<p><strong>이 모든 작업은 하나의 <code class="language-plaintext highlighter-rouge">@Transactional</code> 안에서 수행</strong>된다. 따라서 중간에 예외가 발생하면 비즈니스 데이터와 Outbox row가 함께 롤백된다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Service</span>
<span class="nd">@RequiredArgsConstructor</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">GitLabUserRegistrationService</span> <span class="kd">implements</span> <span class="nc">GitLabUserManagementUseCase</span> <span class="o">{</span>

    <span class="kd">private</span> <span class="kd">final</span> <span class="nc">GitLabUserRegistrationCommandPort</span> <span class="n">commandPort</span><span class="o">;</span>
    <span class="kd">private</span> <span class="kd">final</span> <span class="nc">UserRegistrationEventPublishPort</span> <span class="n">publishPort</span><span class="o">;</span>
    <span class="c1">// ...</span>

    <span class="nd">@Override</span>
    <span class="nd">@Transactional</span>
    <span class="kd">public</span> <span class="nc">GitLabUserRegistrationResult</span> <span class="nf">enqueueBulkUserRegistration</span><span class="o">(</span><span class="nc">GitLabUserRegistrationCommand</span> <span class="n">command</span><span class="o">)</span> <span class="o">{</span>
        <span class="n">validator</span><span class="o">.</span><span class="na">validateNoInProgressRequest</span><span class="o">(</span><span class="n">command</span><span class="o">.</span><span class="na">getToolId</span><span class="o">());</span>

        <span class="c1">// 마스터/상세/사이클/이력 4-테이블 INSERT</span>
        <span class="nc">GitLabUserRegistrationPersistResult</span> <span class="n">persisted</span> <span class="o">=</span>
                <span class="n">commandPort</span><span class="o">.</span><span class="na">saveUserRegistrationRequests</span><span class="o">(</span><span class="n">persistenceMapper</span><span class="o">.</span><span class="na">toPersistenceCommand</span><span class="o">(</span><span class="n">command</span><span class="o">));</span>

        <span class="c1">// 상세 단위 이벤트 → Outbox row 적재 (같은 @Transactional 안)</span>
        <span class="n">publishPort</span><span class="o">.</span><span class="na">publishAll</span><span class="o">(</span><span class="n">eventMapper</span><span class="o">.</span><span class="na">toEvents</span><span class="o">(</span><span class="n">command</span><span class="o">,</span> <span class="n">persisted</span><span class="o">));</span>

        <span class="k">return</span> <span class="k">new</span> <span class="nf">GitLabUserRegistrationResult</span><span class="o">(</span><span class="n">persisted</span><span class="o">.</span><span class="na">getRequestId</span><span class="o">());</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<h3 id="outgoing-port-분리">Outgoing Port 분리</h3>
<hr />
<p>영속 포트와 Outbox 포트는 모두 외부로 나가는 포트지만 책임이 다르다.</p>

<ul>
  <li>영속 포트는 비즈니스 데이터를 저장한다.</li>
  <li>Outbox 포트는 Kafka로 보낼 이벤트를 DB row로 저장한다.</li>
</ul>

<p>이 분리 덕분에 application 계층은 Kafka topic, 직렬화 방식, schema registry 같은 인프라 세부사항을 알 필요가 없다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">interface</span> <span class="nc">GitLabUserRegistrationCommandPort</span> <span class="o">{</span>
    <span class="c1">// 마스터/상세/사이클/이력 4-테이블 일괄 저장</span>
    <span class="nc">GitLabUserRegistrationPersistResult</span> <span class="nf">saveUserRegistrationRequests</span><span class="o">(</span><span class="nc">GitLabUserRegistrationPersistenceCommand</span> <span class="n">cmd</span><span class="o">);</span>
    <span class="c1">// 상태 전이 메서드(markInProgress / markSuccess / markFailure)는 3편 Consumer에서 소개한다</span>
<span class="o">}</span>

<span class="kd">public</span> <span class="kd">interface</span> <span class="nc">UserRegistrationEventPublishPort</span> <span class="o">{</span>
    <span class="c1">// 호출자 @Transactional에 참여 — 절대 새 트랜잭션을 시작하지 않는다</span>
    <span class="kt">void</span> <span class="nf">publishAll</span><span class="o">(</span><span class="nc">List</span><span class="o">&lt;</span><span class="nc">UserRegistrationEvent</span><span class="o">&gt;</span> <span class="n">events</span><span class="o">);</span>
<span class="o">}</span>
</code></pre></div></div>

<h3 id="outbox-row의-의미">Outbox Row의 의미</h3>
<hr />
<p>Outbox row에는 메시징을 위한 정보(topic, message key, payload, 발행 상태)가 저장된다. 이 row는 “이미 발행된 메시지”가 아니라 <strong>나중에 발행해야 할 의도</strong>를 표현한다.</p>

<p>payload는 Kafka로 흘려보낼 메시지 본문을 그대로 직렬화해 둔다. 우리는 Avro 스키마를 그대로 사용해 producer 시점에 한 번 직렬화하고, 폴러는 별도 변환 없이 그 byte를 그대로 전송한다. 트랜잭션 안에서는 schema registry 같은 외부 의존을 호출하지 않는 것이 안전하다.</p>

<h3 id="주의할-점">주의할 점</h3>
<hr />
<p>Outbox adapter가 <strong>별도 트랜잭션을 새로 시작하면 원자성이 깨질 수 있다</strong>. producer의 트랜잭션에 참여하도록 두는 것이 핵심이다.</p>

<p>검증은 단순하다. 저장 직후 예외를 강제로 발생시켰을 때 비즈니스 데이터와 Outbox row가 모두 남지 않아야 한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Component</span>
<span class="nd">@RequiredArgsConstructor</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">UserRegistrationEventPublishAdapter</span> <span class="kd">implements</span> <span class="nc">UserRegistrationEventPublishPort</span> <span class="o">{</span>

    <span class="kd">private</span> <span class="kd">final</span> <span class="nc">EventPublisher</span> <span class="n">eventPublisher</span><span class="o">;</span>
    <span class="kd">private</span> <span class="kd">final</span> <span class="nc">UserRegistrationEventAvroMapper</span> <span class="n">avroMapper</span><span class="o">;</span>

    <span class="nd">@Override</span>
    <span class="c1">// @Transactional 없음 — 호출자(Service)의 트랜잭션에 참여한다</span>
    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">publishAll</span><span class="o">(</span><span class="nc">List</span><span class="o">&lt;</span><span class="nc">UserRegistrationEvent</span><span class="o">&gt;</span> <span class="n">events</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">for</span> <span class="o">(</span><span class="nc">UserRegistrationEvent</span> <span class="n">event</span> <span class="o">:</span> <span class="n">events</span><span class="o">)</span> <span class="o">{</span>
            <span class="n">eventPublisher</span><span class="o">.</span><span class="na">publish</span><span class="o">(</span>
                    <span class="n">event</span><span class="o">.</span><span class="na">getUserRegDmndDtlId</span><span class="o">(),</span>               <span class="c1">// Kafka key = 상세 ID</span>
                    <span class="n">avroMapper</span><span class="o">.</span><span class="na">toAvro</span><span class="o">(</span><span class="n">event</span><span class="o">),</span>
                    <span class="nc">Topics</span><span class="o">.</span><span class="na">GITLAB_USER_REG_COMMAND</span><span class="o">.</span><span class="na">getValue</span><span class="o">());</span>
            <span class="c1">// EventPublisher 내부에서 entityManager.persist(OutboxEventEntity) 수행</span>
            <span class="c1">// → 비즈니스 INSERT와 같은 커밋 단위</span>
        <span class="o">}</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<h3 id="conclusion">Conclusion</h3>
<hr />
<p>이 단계에서 보장되는 것은 <strong>저장과 발행 의도의 원자성</strong>이다. Kafka에 아직 메시지가 도달한 것은 아니지만, 적어도 “저장은 됐는데 발행 의도는 사라지는” 상태는 막을 수 있다.</p>

<h3 id="next-step">Next Step</h3>
<hr />
<p>다음 포스트에서는 <strong><a href="/transactional-outbox-in-practice-thin-poller-and-at-least-once.html">Thin Poller and At-least-once</a></strong>를 다룬다. DB에 남겨둔 발행 의도를 실제 Kafka 메시지로 흘려보내는 얇은 폴러의 역할을 정리한다.</p>]]></content><author><name>HwiRyung Kim</name></author><category term="design" /><summary type="html"><![CDATA[TOC Overview Application Service의 책임 Outgoing Port 분리 Outbox Row의 의미 주의할 점 Conclusion Next Step]]></summary></entry><entry><title type="html">Transactional Outbox in Practice: Prologue</title><link href="https://hrllk.github.io/transactional-outbox-in-practice-prologue.html" rel="alternate" type="text/html" title="Transactional Outbox in Practice: Prologue" /><published>2026-04-19T00:00:00+00:00</published><updated>2026-04-19T00:00:00+00:00</updated><id>https://hrllk.github.io/transactional-outbox-in-practice-prologue</id><content type="html" xml:base="https://hrllk.github.io/transactional-outbox-in-practice-prologue.html"><![CDATA[<h3 id="toc">TOC</h3>
<hr />
<ol>
  <li><a href="#overview">Overview</a></li>
  <li><a href="#문제의-시작">문제의 시작</a></li>
  <li><a href="#outbox-어원">Outbox 어원</a></li>
  <li><a href="#transactional-outbox의-핵심">Transactional Outbox의 핵심</a></li>
  <li><a href="#연재-계획">연재 계획</a></li>
  <li><a href="#약속-our-principle">약속 (Our Principle)</a></li>
  <li><a href="#next-step">Next Step</a></li>
</ol>

<h3 id="overview">Overview</h3>
<hr />
<p>백엔드 서비스에서 DB 저장과 Kafka 발행을 한 흐름으로 묶고 싶을 때가 있다. 하지만 RDB commit과 Kafka ack는 같은 트랜잭션으로 움직이지 않는다.</p>

<p>이번 시리즈는 이 간극을 분산 트랜잭션으로 덮지 않고, <strong>발행 의도를 DB에 먼저 저장하는 Transactional Outbox 패턴</strong>으로 풀어가는 과정을 정리한다.</p>

<h3 id="문제의-시작">문제의 시작</h3>
<hr />
<p>예를 들어 사용자 등록 정보를 DB에 저장한 뒤 Kafka로 후속 처리를 요청하는 흐름이 있다고 하자. DB 저장은 성공했는데 메시지 발행이 실패하거나, 반대로 메시지만 먼저 나가면 운영 데이터는 쉽게 어긋난다.</p>

<p>이 문제는 코드 순서를 조금 바꾼다고 사라지지 않는다. 서로 다른 시스템의 commit 시점이 다르기 때문에 생기는 구조적 문제다.</p>

<h3 id="outbox-어원">Outbox 어원</h3>
<hr />
<p>Outbox는 <strong>outgoing box</strong>의 줄임말로, 밖으로 나갈 메시지를 잠시 보관하는 함을 뜻한다.</p>

<p>사전적으로도 outbox는 “아직 보내지 않은 이메일 메시지를 보관하는 폴더” 또는 “처리 후 외부로 보낼 문서를 담는 책상 위 트레이”를 의미한다. 이름 그대로 이 패턴은 메시지를 즉시 보내는 대신 일단 DB의 outbox 테이블에 담아두고, 나중에 꺼내 보내는 흐름을 따른다.</p>

<h3 id="transactional-outbox의-핵심">Transactional Outbox의 핵심</h3>
<hr />
<p>핵심은 메시지를 즉시 발행하지 않는 것이다. 비즈니스 데이터와 함께 <code class="language-plaintext highlighter-rouge">outbox</code> row를 같은 DB 트랜잭션에 저장하고, 실제 발행은 별도 폴러가 나중에 처리한다.</p>

<pre><code class="language-mermaid">sequenceDiagram
    box Application
        participant Prod as Producer
        participant Poller as Poller
        participant Cons as Consumer
    end
    participant DB
    participant MQ as Message Queue

    Note over Prod,DB: [1편] 원자적 저장
    Prod-&gt;&gt;DB: 비즈니스 데이터 INSERT
    Prod-&gt;&gt;DB: Outbox row INSERT (PENDING)
    Note right of Prod: 같은 @Transactional

    Note over Poller,MQ: [2편] 발행
    loop 폴링
        Poller-&gt;&gt;DB: PENDING row 조회 (FOR UPDATE SKIP LOCKED)
        DB--&gt;&gt;Poller: rows
        Poller-&gt;&gt;MQ: Kafka 발행
        Poller-&gt;&gt;DB: 상태 PENDING → SENT
    end

    Note over MQ,Cons: [3편] 소비
    MQ--&gt;&gt;Cons: 메시지 수신 (at-least-once)
    Cons-&gt;&gt;DB: 멱등 상태 전이 (WAIT → SCS / FAIL)
</code></pre>

<p>이렇게 하면 저장과 발행 의도는 원자적으로 묶이고, Kafka 발행 실패는 재시도 가능한 별도 단계로 분리된다.</p>

<h3 id="연재-계획">연재 계획</h3>
<hr />
<ol>
  <li><a href="/transactional-outbox-in-practice-prologue.html">[프롤로그] DB와 MQ 사이의 균열, 그리고 Outbox라는 선택 (현재 글)</a></li>
  <li><a href="/transactional-outbox-in-practice-atomic-persist-and-enqueue.html">[1편] Atomic Persist and Enqueue: 저장과 발행 의도를 같은 트랜잭션에 묶기</a></li>
  <li><a href="/transactional-outbox-in-practice-thin-poller-and-at-least-once.html">[2편] Thin Poller and At-least-once: 얇은 폴러와 중복 발행을 받아들이는 설계</a></li>
  <li><a href="/transactional-outbox-in-practice-idempotent-consumer.html">[3편] Idempotent Consumer: 중복 메시지를 흡수하는 소비자 만들기</a></li>
  <li><a href="/transactional-outbox-in-practice-operating-in-production.html">[4편] Operating in Production: 멀티 인스턴스와 운영 이슈 정리</a></li>
</ol>

<h3 id="약속-our-principle">약속 (Our Principle)</h3>
<hr />
<ul>
  <li><strong>원자성:</strong> 비즈니스 데이터와 발행 의도는 같은 DB 트랜잭션에 묶는다.</li>
  <li><strong>분리:</strong> 발행과 소비는 각각 별도 책임으로 다룬다.</li>
  <li><strong>멱등성:</strong> at-least-once 발행으로 생기는 중복은 컨슈머가 흡수한다.</li>
</ul>

<h3 id="next-step">Next Step</h3>
<hr />
<p>다음 포스트에서는 <a href="/transactional-outbox-in-practice-atomic-persist-and-enqueue.html">Atomic Persist and Enqueue</a>를 다룬다. Outbox 패턴의 출발점인 “저장과 발행 의도 적재”를 어떤 코드 경계 안에 둬야 하는지 정리한다.</p>]]></content><author><name>HwiRyung Kim</name></author><category term="design" /><summary type="html"><![CDATA[TOC Overview 문제의 시작 Outbox 어원 Transactional Outbox의 핵심 연재 계획 약속 (Our Principle) Next Step]]></summary></entry><entry><title type="html">Modern Beautiful API Response Design: Exception Translation Layer</title><link href="https://hrllk.github.io/modern-beautiful-api-response-design-exception-translation-layer.html" rel="alternate" type="text/html" title="Modern Beautiful API Response Design: Exception Translation Layer" /><published>2026-03-18T00:00:00+00:00</published><updated>2026-03-18T00:00:00+00:00</updated><id>https://hrllk.github.io/modern-beautiful-api-response-design-exception-translation-layer</id><content type="html" xml:base="https://hrllk.github.io/modern-beautiful-api-response-design-exception-translation-layer.html"><![CDATA[<h3 id="toc">TOC</h3>
<hr />
<ol>
  <li><a href="#overview">Overview</a></li>
  <li><a href="#왜-번역-계층이-따로-필요한가">왜 번역 계층이 따로 필요한가</a></li>
  <li><a href="#설계-목표">설계 목표</a></li>
  <li><a href="#1-번역-책임은-프레젠테이션-계층에-둔다">1. 번역 책임은 프레젠테이션 계층에 둔다</a></li>
  <li><a href="#2-advisor는-내부-계약을-외부-계약으로-바꾼다">2. Advisor는 내부 계약을 외부 계약으로 바꾼다</a></li>
  <li><a href="#3-번역-계층이-하면-안-되는-일">3. 번역 계층이 하면 안 되는 일</a></li>
  <li><a href="#closing">Closing</a></li>
</ol>

<h3 id="overview">Overview</h3>
<hr />
<p>앞선 글에서 내부 예외 모델과 외부 에러 계약을 분리했다. 이제 남은 질문은 하나다. <strong>이 둘을 어디서 연결할 것인가?</strong></p>

<p>이 글에서는 <code class="language-plaintext highlighter-rouge">ProblemDetail</code> 같은 외부 응답 계약을 실제로 누가 만들고, 왜 그 책임을 프레젠테이션 계층에 두어야 하는지를 다룬다.</p>

<h3 id="왜-번역-계층이-따로-필요한가">왜 번역 계층이 따로 필요한가</h3>
<hr />
<p>내부 예외 모델과 외부 응답 계약은 역할이 다르다.</p>

<ul>
  <li>내부는 아키텍처와 도메인 책임을 지키기 위한 모델이다.</li>
  <li>외부는 클라이언트와의 안정적인 통신 계약이다.</li>
</ul>

<p>이 둘을 같은 계층에서 처리하면 내부 설계가 외부 포맷에 끌려가고, 외부 포맷 변경이 내부 코드까지 흔들게 된다. 그래서 중간에 <strong>번역 계층</strong>이 필요하다.</p>

<h3 id="설계-목표">설계 목표</h3>
<hr />
<ol>
  <li>내부 예외 모델은 순수하게 유지한다.</li>
  <li>외부 응답 계약은 프레젠테이션 계층에서만 조립한다.</li>
  <li>번역 계층은 비즈니스 판단 없이 변환만 수행한다.</li>
</ol>

<h3 id="1-번역-책임은-프레젠테이션-계층에-둔다">1. 번역 책임은 프레젠테이션 계층에 둔다</h3>
<hr />
<p>헥사고날 아키텍처 기준으로 HTTP 응답은 프레젠테이션 어댑터의 책임이다. 따라서 예외를 <code class="language-plaintext highlighter-rouge">ProblemDetail</code>이나 공통 API 에러 포맷으로 바꾸는 일도 프레젠테이션 계층이 맡아야 한다.</p>

<p>즉:</p>

<ul>
  <li>도메인 계층은 예외를 던진다.</li>
  <li>애플리케이션 계층은 흐름을 제어하다가 예외를 던진다.</li>
  <li>프레젠테이션 계층은 그 예외를 HTTP 응답으로 번역한다.</li>
</ul>

<p>그래서 <code class="language-plaintext highlighter-rouge">@RestControllerAdvice</code>는 단순 편의 기능이 아니라, 아키텍처적으로도 자연스러운 번역 지점이다.</p>

<h3 id="2-advisor는-내부-계약을-외부-계약으로-바꾼다">2. Advisor는 내부 계약을 외부 계약으로 바꾼다</h3>
<hr />
<p><code class="language-plaintext highlighter-rouge">Advisor</code>의 역할은 새 규칙을 만드는 것이 아니라, 이미 정의된 내부 계약을 외부 계약으로 <strong>번역</strong>하는 것이다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@RestControllerAdvice</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">GlobalExceptionAdvisor</span> <span class="o">{</span>

    <span class="nd">@ExceptionHandler</span><span class="o">(</span><span class="nc">ApplicationException</span><span class="o">.</span><span class="na">class</span><span class="o">)</span>
    <span class="kd">public</span> <span class="nc">ProblemDetail</span> <span class="nf">handleApplicationException</span><span class="o">(</span><span class="nc">ApplicationException</span> <span class="n">e</span><span class="o">)</span> <span class="o">{</span>
        <span class="nc">HttpStatus</span> <span class="n">status</span> <span class="o">=</span> <span class="n">determineHttpStatus</span><span class="o">(</span><span class="n">e</span><span class="o">.</span><span class="na">getErrorCode</span><span class="o">());</span>

        <span class="nc">ProblemDetail</span> <span class="n">problem</span> <span class="o">=</span> <span class="nc">ProblemDetail</span><span class="o">.</span><span class="na">forStatus</span><span class="o">(</span><span class="n">status</span><span class="o">);</span>
        <span class="n">problem</span><span class="o">.</span><span class="na">setTitle</span><span class="o">(</span><span class="s">"Application error"</span><span class="o">);</span>
        <span class="n">problem</span><span class="o">.</span><span class="na">setDetail</span><span class="o">(</span><span class="n">e</span><span class="o">.</span><span class="na">getMessage</span><span class="o">());</span>
        <span class="n">problem</span><span class="o">.</span><span class="na">setType</span><span class="o">(</span><span class="no">URI</span><span class="o">.</span><span class="na">create</span><span class="o">(</span><span class="s">"https://api.example.com/problems/"</span> <span class="o">+</span> <span class="n">e</span><span class="o">.</span><span class="na">getSpecificCode</span><span class="o">()));</span>
        <span class="n">problem</span><span class="o">.</span><span class="na">setProperty</span><span class="o">(</span><span class="s">"code"</span><span class="o">,</span> <span class="n">e</span><span class="o">.</span><span class="na">getSpecificCode</span><span class="o">());</span>
        <span class="n">problem</span><span class="o">.</span><span class="na">setProperty</span><span class="o">(</span><span class="s">"source"</span><span class="o">,</span> <span class="s">"application"</span><span class="o">);</span>

        <span class="k">return</span> <span class="n">problem</span><span class="o">;</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>핵심은 두 가지다.</p>

<ol>
  <li>상태 코드는 여전히 내부의 <code class="language-plaintext highlighter-rouge">ErrorCode</code>를 기준으로 결정한다.</li>
  <li>클라이언트 식별 코드는 <code class="language-plaintext highlighter-rouge">ProblemSpec</code>이 제공한 값을 응답에 싣는다.</li>
</ol>

<p>즉 <code class="language-plaintext highlighter-rouge">Advisor</code>는 내부 계약을 외부 계약으로 바꿀 뿐, 새로운 비즈니스 판단을 하지 않는다.</p>

<h3 id="3-번역-계층이-하면-안-되는-일">3. 번역 계층이 하면 안 되는 일</h3>
<hr />
<p>번역 계층이 과도하게 똑똑해지면 다시 책임이 섞인다. <code class="language-plaintext highlighter-rouge">Advisor</code>는 아래 일을 하면 안 된다.</p>

<ul>
  <li>도메인 규칙을 새로 판단하는 일</li>
  <li>예외를 보고 비즈니스 흐름을 다시 결정하는 일</li>
  <li>원래 없던 식별 코드를 임의로 만드는 일</li>
</ul>

<p>번역 계층은 이미 만들어진 내부 계약을 받아, 외부 포맷으로 안정적으로 바꾸는 데만 집중해야 한다.</p>

<h3 id="closing">Closing</h3>
<hr />
<p>시리즈를 정리하면 역할은 이렇게 나뉜다.</p>

<ul>
  <li><strong>Internal Exception Model</strong>: 서버 내부에서 실패를 어떻게 모델링할 것인가</li>
  <li><strong>External Error Contract</strong>: 클라이언트에게 어떤 에러 계약을 보여줄 것인가</li>
  <li><strong>Exception Translation Layer</strong>: 그 둘을 어디서, 어떻게 연결할 것인가</li>
</ul>

<p>이렇게 나누면 내부 설계와 외부 계약이 서로를 덜 오염시키고, 각 계층의 책임도 훨씬 선명해진다.</p>]]></content><author><name>HwiRyung Kim</name></author><category term="design" /><summary type="html"><![CDATA[TOC Overview 왜 번역 계층이 따로 필요한가 설계 목표 1. 번역 책임은 프레젠테이션 계층에 둔다 2. Advisor는 내부 계약을 외부 계약으로 바꾼다 3. 번역 계층이 하면 안 되는 일 Closing]]></summary></entry><entry><title type="html">Modern Beautiful API Response Design: External Error Contract</title><link href="https://hrllk.github.io/modern-beautiful-api-response-design-external-error-contract.html" rel="alternate" type="text/html" title="Modern Beautiful API Response Design: External Error Contract" /><published>2026-03-17T00:00:00+00:00</published><updated>2026-03-17T00:00:00+00:00</updated><id>https://hrllk.github.io/modern-beautiful-api-response-design-external-error-contract</id><content type="html" xml:base="https://hrllk.github.io/modern-beautiful-api-response-design-external-error-contract.html"><![CDATA[<h3 id="toc">TOC</h3>
<hr />
<ol>
  <li><a href="#overview">Overview</a></li>
  <li><a href="#왜-에러-응답도-계약이어야-하는가">왜 에러 응답도 계약이어야 하는가</a></li>
  <li><a href="#설계-목표">설계 목표</a></li>
  <li><a href="#1-내부-예외와-외부-응답은-분리되어야-한다">1. 내부 예외와 외부 응답은 분리되어야 한다</a></li>
  <li><a href="#2-왜-problemdetail인가">2. 왜 ProblemDetail인가</a></li>
  <li><a href="#3-어떤-정보를-응답에-담아야-하는가">3. 어떤 정보를 응답에 담아야 하는가</a></li>
  <li><a href="#4-응답-필드의-책임-분리">4. 응답 필드의 책임 분리</a></li>
  <li><a href="#closing">Closing</a></li>
</ol>

<h3 id="overview">Overview</h3>
<hr />
<p>앞선 글에서는 서버 내부에서 실패를 어떻게 모델링할지 정리했다. 공통 부모 예외, 계층별 예외, <code class="language-plaintext highlighter-rouge">ErrorCode</code>, <code class="language-plaintext highlighter-rouge">ProblemSpec</code>까지 모두 내부 계약의 일부다.</p>

<p>하지만 내부 예외 계약이 잘 설계되어 있어도, 최종적으로 클라이언트가 받는 응답 포맷이 일관되지 않으면 계약은 다시 흔들린다. 그래서 이 단계에서는 예외 자체보다 <strong>응답 번역 규격</strong>이 핵심이 된다.</p>

<p>이 글에서는 내부 예외를 외부 API 에러 응답으로 어떻게 번역할지, 그리고 왜 <code class="language-plaintext highlighter-rouge">ProblemDetail</code>이 이 번역의 중심이 되어야 하는지를 다룬다.</p>

<h3 id="왜-에러-응답도-계약이어야-하는가">왜 에러 응답도 계약이어야 하는가</h3>
<hr />
<p>에러 응답을 즉흥적으로 만들면 보통 아래 문제가 발생한다.</p>

<ol>
  <li>예외마다 JSON 구조가 달라진다.</li>
  <li>HTTP 상태 코드와 본문 의미가 어긋난다.</li>
  <li>클라이언트가 <code class="language-plaintext highlighter-rouge">message</code> 문구에 의존해 분기하게 된다.</li>
  <li>내부 예외 구조가 외부 계약에 그대로 새어 나온다.</li>
</ol>

<p>즉, 내부 설계와 외부 계약 사이에 <strong>번역 계층</strong>이 없으면 응답은 오래 버티지 못한다.</p>

<h3 id="설계-목표">설계 목표</h3>
<hr />
<p>이번 단계의 목표는 명확하다.</p>

<ol>
  <li>내부 예외와 외부 응답 포맷을 분리한다.</li>
  <li>외부 응답은 하나의 표준 구조로 통일한다.</li>
  <li>상태 코드, 식별 코드, 사용자 메시지의 역할을 분리한다.</li>
  <li>번역 책임은 프레젠테이션 계층에서만 담당한다.</li>
</ol>

<h3 id="1-내부-예외와-외부-응답은-분리되어야-한다">1. 내부 예외와 외부 응답은 분리되어야 한다</h3>
<hr />
<p><code class="language-plaintext highlighter-rouge">ApplicationException</code>, <code class="language-plaintext highlighter-rouge">DomainException</code>, <code class="language-plaintext highlighter-rouge">InfrastructureException</code>은 모두 <strong>서버 내부 모델</strong>이다. 이 객체들은 서비스 코드와 아키텍처 책임 분리를 위해 존재한다.</p>

<p>반면 클라이언트가 받아야 하는 것은 서버 내부 클래스 이름이 아니라, <strong>안정적인 외부 계약</strong>이다.<br />
즉 내부에서는:</p>

<ul>
  <li>어떤 계층에서 실패했는가</li>
  <li>어떤 상태 범주인가</li>
  <li>어떤 <code class="language-plaintext highlighter-rouge">ProblemSpec</code>을 사용하는가</li>
</ul>

<p>를 다루고, 외부에서는:</p>

<ul>
  <li>HTTP 상태 코드</li>
  <li>문제 식별자</li>
  <li>사용자 메시지</li>
  <li>요청 문맥</li>
</ul>

<p>을 다뤄야 한다.</p>

<p>이 둘을 같은 객체로 처리하려고 하면 내부 구조가 외부로 새고, 외부 포맷 변경이 내부 설계까지 흔들게 된다.</p>

<h3 id="2-왜-problemdetail인가">2. 왜 ProblemDetail인가</h3>
<hr />
<p>스프링 6부터는 에러 응답을 표현하는 표준 구조로 <code class="language-plaintext highlighter-rouge">ProblemDetail</code>을 제공한다. 이 객체는 HTTP 에러 응답을 일관되게 만들기 위한 최소 골격을 제공한다.</p>

<p>핵심은 단순하다.</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">status</code>: HTTP 상태 코드</li>
  <li><code class="language-plaintext highlighter-rouge">title</code>: 문제의 짧은 이름</li>
  <li><code class="language-plaintext highlighter-rouge">detail</code>: 사람이 읽을 설명</li>
  <li><code class="language-plaintext highlighter-rouge">type</code>: 문제 유형 식별자</li>
  <li><code class="language-plaintext highlighter-rouge">properties</code>: 팀이 추가로 넣고 싶은 확장 필드</li>
</ul>

<p>즉 <code class="language-plaintext highlighter-rouge">ProblemDetail</code>은 “에러 응답의 기본 뼈대”를 제공하고, 우리 시스템은 그 위에 <code class="language-plaintext highlighter-rouge">code</code>, <code class="language-plaintext highlighter-rouge">source</code> 같은 팀 규칙을 얹으면 된다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">ProblemDetail</span> <span class="n">problem</span> <span class="o">=</span> <span class="nc">ProblemDetail</span><span class="o">.</span><span class="na">forStatus</span><span class="o">(</span><span class="nc">HttpStatus</span><span class="o">.</span><span class="na">NOT_FOUND</span><span class="o">);</span>
<span class="n">problem</span><span class="o">.</span><span class="na">setTitle</span><span class="o">(</span><span class="s">"Resource not found"</span><span class="o">);</span>
<span class="n">problem</span><span class="o">.</span><span class="na">setDetail</span><span class="o">(</span><span class="s">"User not found"</span><span class="o">);</span>
<span class="n">problem</span><span class="o">.</span><span class="na">setType</span><span class="o">(</span><span class="no">URI</span><span class="o">.</span><span class="na">create</span><span class="o">(</span><span class="s">"https://api.example.com/problems/user-not-found"</span><span class="o">));</span>
<span class="n">problem</span><span class="o">.</span><span class="na">setProperty</span><span class="o">(</span><span class="s">"code"</span><span class="o">,</span> <span class="s">"USER-404"</span><span class="o">);</span>
<span class="n">problem</span><span class="o">.</span><span class="na">setProperty</span><span class="o">(</span><span class="s">"source"</span><span class="o">,</span> <span class="s">"application"</span><span class="o">);</span>
</code></pre></div></div>

<h3 id="3-어떤-정보를-응답에-담아야-하는가">3. 어떤 정보를 응답에 담아야 하는가</h3>
<hr />
<p>여기서 중요한 것은 “많이 담는 것”이 아니라, <strong>각 필드가 무엇을 책임지는지 분리하는 것</strong>이다.</p>

<ul>
  <li><strong>HTTP status</strong>: 전송 계층의 의미다. 요청이 실패했는지, 권한 문제인지, 서버 문제인지 표현한다.</li>
  <li><strong><code class="language-plaintext highlighter-rouge">type</code></strong>: 문제 유형의 안정적인 식별자다.</li>
  <li><strong><code class="language-plaintext highlighter-rouge">code</code></strong>: 프론트엔드나 운영 도구가 분기하기 쉬운 팀 내부 규격 코드다.</li>
  <li><strong><code class="language-plaintext highlighter-rouge">detail</code></strong>: 사람이 읽을 수 있는 설명이다.</li>
  <li><strong><code class="language-plaintext highlighter-rouge">source</code></strong>: 선택적이다. 어느 계층에서 번역되었는지 드러내고 싶을 때만 둔다.</li>
</ul>

<p>즉 같은 “에러”라도 역할은 다르다.</p>

<ul>
  <li>상태 코드는 HTTP를 위한 것</li>
  <li>식별 코드는 클라이언트 분기를 위한 것</li>
  <li>메시지는 사람을 위한 것</li>
</ul>

<p>이 셋을 하나의 필드로 뭉개면 다시 문자열 파싱 구조로 되돌아간다.</p>

<h3 id="4-응답-필드의-책임-분리">4. 응답 필드의 책임 분리</h3>
<hr />
<p>앞선 두 글과 이번 글을 합치면 역할은 이렇게 정리된다.</p>

<ul>
  <li><strong>Exception Hierarchy</strong>: 실패의 출처와 책임을 분리한다.</li>
  <li><strong><code class="language-plaintext highlighter-rouge">ErrorCode</code></strong>: 각 계층이 가질 수 있는 본질적인 상태 범주를 표현한다.</li>
  <li><strong><code class="language-plaintext highlighter-rouge">ProblemSpec</code></strong>: 외부에 노출할 식별 코드와 메시지 키를 관리한다.</li>
  <li><strong><code class="language-plaintext highlighter-rouge">ProblemDetail</code></strong>: 외부 에러 응답 계약의 기본 뼈대를 제공한다.</li>
</ul>

<p>즉 내부에서는 아키텍처를 지키고, 외부에서는 표준 응답 포맷을 지키는 구조다. 이 둘을 하나로 합치지 않고, 번역 지점을 둔 것이 핵심이다.</p>

<h3 id="closing">Closing</h3>
<hr />
<p>예외 계층과 <code class="language-plaintext highlighter-rouge">ErrorCode</code>, <code class="language-plaintext highlighter-rouge">ProblemSpec</code>이 내부 계약의 뼈대라면, <code class="language-plaintext highlighter-rouge">ProblemDetail</code>은 그 계약을 외부 세계로 내보내는 표준 포맷이다. 내부 설계와 외부 응답을 분리해 두면, 서버는 더 안정적으로 진화하고 클라이언트는 더 예측 가능한 계약 위에서 동작할 수 있다.</p>

<p>다음 글에서는 이 외부 계약을 실제로 <a href="/modern-beautiful-api-response-design-exception-translation-layer.html">어디서, 어떻게 번역해야 하는지</a> 다룬다. 즉 <code class="language-plaintext highlighter-rouge">@RestControllerAdvice</code>와 번역 계층의 위치를 정리한다.</p>]]></content><author><name>HwiRyung Kim</name></author><category term="design" /><summary type="html"><![CDATA[TOC Overview 왜 에러 응답도 계약이어야 하는가 설계 목표 1. 내부 예외와 외부 응답은 분리되어야 한다 2. 왜 ProblemDetail인가 3. 어떤 정보를 응답에 담아야 하는가 4. 응답 필드의 책임 분리 Closing]]></summary></entry></feed>